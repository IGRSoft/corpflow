"""benchmark/live — live full-pipeline dispatch adapter (DV0e, sole owner).

This subtree is reached ONLY via `benchmark/run-benchmark.sh --live` (AC-8). The
default `make benchmark` / `make test` paths never import anything here.
"""

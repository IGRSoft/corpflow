You are the DV (Development) stage of a worktask building a benchmark workload.

TASK: Implement the real, runnable SwiftUI multiplatform Tic-Tac-Toe app per the
design. Implement the macOS and iOS platform layers (the package targets macOS 15+
AND iOS 18+); apply SwiftUI, Swift Testing, and accessibility best practices.
- Sources/TicTacToeKit: Engine (Board move-validation/win/draw, AIOpponent easy
  random + hard minimax with injectable RNG), Models (ScoreEntry/Leaderboard/
  GameSettings, Codable, injectable store URLs), Services (SoundPlaying protocol,
  NSSound/AudioToolbox impls, SilentSoundPlayer), Navigation (AppScreen/AppRouter
  @Observable), ViewModels (@MainActor GameViewModel), Views (RootView/MainMenu/
  Game/BoardCell/Leaderboard/Settings, animated transitions, accessibility labels).
- Sources/tictactoe: executable — SwiftUI App by default, `--cli` text loop,
  plus the scripted mode below.

SCRIPTED CLI CONTRACT — implement exactly; conformance is graded:
`tictactoe --moves 0,4,1` plays those cell indices in order as alternating
turns starting with X, stopping early once the game is won or drawn. It then
prints the board followed by a `result:` line, e.g. for no moves at all:

 0 | 1 | 2
---+---+---
 3 | 4 | 5
---+---+---
 6 | 7 | 8
result: in_progress

- a played cell shows `X` or `O`; an unplayed cell shows its own index digit
- every board row has one leading and one trailing space, as shown
- the last line is `result: ` plus one of `in_progress`, `X_wins`, `O_wins`,
  or `draw`
- exit 0 when the sequence played; 1 when a move is invalid (occupied cell or
  outside 0..8); 2 when --moves is not a comma-separated integer list
- diagnostics for exit 1 and 2 go to stderr; their wording is not graded
- whitespace around move tokens is accepted (`0, 4, 1` is valid)

- Tests: Swift Testing (@Test/#expect) suites for engine, models, router, and
  view-model; `swift test` must PASS headless.
Use Swift 6 + SwiftPM + Apple SDKs ONLY. No third-party packages, no network.
Write real, runnable source into the workdir. Record what you built in
.context/development-0.md and STOP.
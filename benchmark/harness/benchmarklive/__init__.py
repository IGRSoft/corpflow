"""BenchmarkLive — live full-pipeline dispatch (budget, credentials, dispatch, capture,
preamble). Reached ONLY via bench-live (AC-8 import isolation); benchmarkkit and the
deterministic entrypoints never import this package.
"""

# TicTacToe — moved

This directory held a byte-identical copy of the benchmark fixture package.
The canonical source is [`benchmark/ttt-template/`](../../benchmark/ttt-template/) —
build, test and coverage all run against it (`make test-swift`, `make coverage`).

The copy was removed because nothing referenced it and it had to be
hand-regenerated whenever the template changed.

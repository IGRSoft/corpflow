{"case_id":14,"verdict":"pass","note":"Opens \"Found the surface: hooks/megatask-monitor.sh\" with its registration (.claude-plugin/plugin.json:139), test file, and header contract \"always exit 0 and never block a turn\", then argues the hang violates that contract so it is a real defect. Notes no .worktrees/orchestrator.json exist in this checkout, so P0 is diagnosis-first: separate a hung hook process from a subagent that never emits SubagentStop, which \"looks identical from the outside\". Full template, S-M / 10, --emergency off the live-failure framing. Risks bar manual state repair outside the hook's own jq + atomic_write pattern.","split":"dev","type":"incident","grounding":"obvious","route":"emerg","harness_status":"pass","harness_failed":[]}
{"case_id":21,"verdict":"fail","note":"Disputes the mechanism but not the premise. States the deterministic functions \"are pinned byte-for-byte and not the source of drift\", then affirms the drift itself and plans against it: two free-text titles (commands/worktask.md:297 Step 3c and refine-branch-target.sh Step A.4b) feed derive_type's keyword buckets (branch-lib.sh:114-140), so one request can end bugfix/ then feature/. Reaches the grounded branch-lib.sh and cites gate 6 at 312-330. Renders the full plan template and a /worktask trigger for the work it was expected to refute.","split":"dev","type":"bug","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["disputes-the-premise"]}
{"case_id":23,"verdict":"pass","note":"Names the grounded hooks/anchor-preflight.sh in the preamble (\"that's the surface\"), but sites the fix elsewhere: a live wrong anchor in stage-contracts.md #tpl-st line 648 (complete-summary-N.md#decision where ST's canonical artifact is retrospective-N.md), caught against the correct open_questions[].ref two lines below. Enumerates three existing guards and the gap between them (frontmatter_template_lint structure-only, artifact-map-parity basenames, agent_section_lint H2s). New lint lands in cache-lint.sh/cache-lint.bats rather than the preflight hook. S / 6, P2 \"none\", standard tier with --emergency explicitly ruled out.","split":"dev","type":"bug","grounding":"buried","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":33,"verdict":"fail","note":"Misses the designated surface. Finds skills/agent-coordination/SKILL.md Stall Timeout (a global 10-minute inactivity guard at :419) and a real gap - the Retry/Escalate Matrix at :73-96 has no stalled row to escalate into - then plans a per-stage table on top of it. Never opens hooks/megatask-monitor.sh, which trace 14 in this same run shows is registered as a Stop/SubagentStop hook with no timeout guard around its jq/atomic_write at all. Template, S / 7, and standard-tier routing are otherwise clean.","split":"dev","type":"feature","grounding":"adjacent","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":36,"verdict":"fail","note":"Planned where the case expects a question. Grep-confirms no Slack integration exists, then resolves the scope ambiguity itself: \"'each stage completes' means every stage agent's SubagentStop (all 9+ stages)\" and states \"Flagging it here rather than as a separate question since it only changes scope, not viability.\" Never asks which channel, workspace, or webhook. The plan itself is well grounded - copies hooks/audit-subagent.sh's shape, reuses the dedupe_key convention, keeps the webhook in an env var, and correctly declines to raise the tier for a surface it discovered rather than was given.","split":"dev","type":"feature","grounding":"absent","route":"std","harness_status":"fail","harness_failed":["should-have-asked-not-planned"]}
{"case_id":38,"verdict":"pass","note":"Asks instead of planning and says why. \"No settings screen or UI app exists in this repo\", cites the skill's rule 1 for the no-surface-at-all branch, and renders no plan template and no /worktask trigger. Two marked questions: whether this belongs to a different repository, or is a demo request against /request-plan itself. Closes by offering to gather context once pointed at a surface.","split":"dev","type":"feature","grounding":"absent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":43,"verdict":"pass","note":"Harness scored this as planning-instead-of-asking, but it never plans. Confirms \"no package.json, no .tsx/.jsx files, no frontend directory anywhere\", cites the skill's step 1 no-surface branch, renders no template and no /worktask trigger, and states \"I'm not producing a generic React-refactor plan with invented phases for a codebase that isn't here.\" The two asks are imperative rather than interrogative - \"point me at it (path or repo name)\", \"say so and I'll scope a plan against that instead\" - and the response contains no question mark, unlike trace 38 which passed. Same behavior, different punctuation.","split":"dev","type":"refactor","grounding":"absent","route":"std","harness_status":"fail","harness_failed":["should-have-asked-not-planned"]}
{"case_id":50,"verdict":"pass","note":"Root-causes past the file named in the request. Reproduces the asymmetry directly - format_cost_delta(1.0005, 1.0) gives \"+0.00\" while format_cost_delta(1.0, 1.0005) gives \"0.00\" - and locates it in formatting.py:31-39, where the sign is re-added from the raw sign of d after format_cost has already suppressed it on the rendered value. Keeps report.py:285 as the user-visible symptom and clears the other grounded file, analysis.py, as calling format_cost directly. XS / 2, P2 \"none\". Risks flag golden fixtures that may assert the buggy \"+0.00\".","split":"dev","type":"bug","grounding":"obvious","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":60,"verdict":"pass","note":"Refutes with specifics and stops. Names commit 73765a1, the SR0 phase, the \"### Threat Model (SR0)\" section, the \"## threat-model\" artifact anchor, and the procedure in skills/security-review-process/references/threat-model.md including its documented \"no material threat surface\" pass case. Invokes the skill's section 4 exception explicitly, renders no plan template and no /worktask trigger, and offers to scope a narrower extension instead.","split":"dev","type":"feature","grounding":"obvious","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":65,"verdict":"fail","note":"Goes to the orchestrator's audit trail instead of the benchmark harness. Grounds in skills/cost-optimization/SKILL.md, hooks/model-switch-audit.sh and the live .context/logs/audit.jsonl, then states \"there's no single place that takes two run IDs and explains a cost delta; you'd correlate /cost's per-model breakdown against audit.jsonl by hand\" - while the designated surface is benchmark/harness/benchmarkkit/analysis.py, which trace 50 in this same run describes as calling format_cost directly for its per-stage cost table. Plans a new inference-based diagnostic on proxies (off_tier, retry, effort) because \"audit rows carry duration_ms and effort, but no token counts or dollar cost\".","split":"dev","type":"feature","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":70,"verdict":"fail","note":"Cites the wrong agent file. Grounds the method in \"agents/project-manager.md already lists backlog prioritization (MoSCoW, WSJF, RICE) as owned expertise\"; the designated surface is agents/product-manager.md. Otherwise concrete: pulls the two live open issues (#359 complexity:high, #353 bug, neither carrying a value/effort score), and flags its own n=2 problem - \"a full scoring framework may be overkill for two items\". Template, XS-S / 7 with the five-driver breakdown, P2 \"none\", standard tier.","split":"dev","type":"feature","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":84,"verdict":"fail","note":"Never opens the designated hooks/megatask-monitor.sh, but surfaces something arguably larger: /worktask-status and skills/worktask/scripts/status-view.sh (329 lines, with bats) already implemented this request and were deleted in commit 41f044f (v4.0.26, 2026-08-26) - \"not for being wrong\" but as \"working code with zero pipeline references\". Plans a revival, remapping fields against the current schemas and adding a real citation so the orphan-script lint can't cut it again. Marks that three releases since added progress{}, last_error{}, completed_via and worktree.* that a verbatim restore \"would compile but silently under-report\". Leaks orchestration narration into the deliverable: \"I've kicked off a research pass\", \"cancelled it\", \"I'll just wait for the research agent's completion notification\".","split":"dev","type":"feature","grounding":"adjacent","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":103,"verdict":"pass","note":"Names the mechanism, not just the file. Reads both enforcement hooks and locates the gap in the grounded hooks/comment-standard-context.sh: the PostToolUse reminder writes a sentinel on the first source-file touch and no-ops on every later one ([ -e \"$_sentinel\" ] && return 0), while dv-comment-density-gate.sh only fires at SubagentStop - \"front-loaded ... and back-loaded ..., with nothing reinforcing the rule during a long edit session in between. That gap is exactly the reported symptom.\" Scopes out both rewriting the density gate and editing the standard itself: \"the rule isn't wrong, its reinforcement cadence is.\" S / 7, P2 \"none\". Trails one offer to save the plan to .context/request-plan-0.md after the trigger.","split":"dev","type":"feature","grounding":"buried","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":108,"verdict":"pass","note":"P0 is read the validator's own error before acting: run handoff-harness.sh --validate-frontmatter to get the literal fail: reason, because \"the harness's own error text names the fix - guessing before reading it wastes a retry\". States that no active .context/state.json exists here, so this is the failure-mode response rather than a fix bound to one run. Risks separate the paths that collapse under misclassification - missing_input escalates to the previous stage while logic retries the same agent, retry_count == 3 means exhausted - and note sweep-stub ledger divergence \"has no auto-resolution\" and is manual. --emergency off the still-failing framing. XS-S / 4-7.","split":"dev","type":"incident","grounding":"buried","route":"emerg","harness_status":"pass","harness_failed":[]}
{"case_id":110,"verdict":"pass","note":"Opens with \"Found the surface: skills/worktask/scripts/fn-preflight-cmds.sh\" and its caller chain, then reads git log: three commits (4e205e2, 39aa30d, 0bd7df5, all #356, all 2026-09-04) each fixed a distinct defect in this one file inside two days, \"so a fourth one blocking PRs now is plausible\". Declines to name a cause it cannot see: \"this worktree's own .context/logs/audit.jsonl has no fn_preflight/pr_body_gate/base_sanity/staging rows at all - nothing here confirms which check is actually tripping\". P0 reproduces before editing. Flags FN_BASE_SANITY_OVERRIDE as a sanctioned escape hatch that \"would mask the defect instead of fixing it\". S-M / 17 with the five-driver split, --emergency.","split":"dev","type":"incident","grounding":"obvious","route":"emerg","harness_status":"pass","harness_failed":[]}
{"case_id":113,"verdict":"pass","note":"Tests the premise before accepting it. Finds the generic agent-facing checklists.md Deployment Readiness Checklist and rules it insufficient rather than refuting the request: \"There is no standalone, human-readable policy doc stating what actually gates a release for this project - the premise holds for a project-level written policy, even though an internal template exists nearby.\" Names this repo's real gates (.github/workflows/test.yml, the *-selftest.sh scripts, test-execution-gate.sh and siblings, version-bump-from-git.sh) against the boilerplate it is replacing (\"Code coverage >= threshold (e.g., 80%)\"), and cross-links the new doc from the grounded release-engineering/SKILL.md and release-engineer.md. Risks flag writing down a gate nothing enforces. S / 7, P2 \"none\".","split":"dev","type":"docs","grounding":"adjacent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":120,"verdict":"pass","note":"Asks rather than inventing infrastructure. \"No Redis or session infrastructure found in this repo\", with the search stated - no redis, no session-store, no docker-compose/IaC, \"only incidental keyword hits in test fixtures and doc examples\". No template, no trigger. Asks which repo the cluster is in and for the on-call runbook, and keeps the urgency live by pointing at /worktask --emergency against the right repo rather than absorbing it here.","split":"dev","type":"incident","grounding":"absent","route":"emerg","harness_status":"pass","harness_failed":[]}
{"case_id":137,"verdict":"pass","note":"Lands on the grounded file and quotes it by line: headless-dispatch.md:1-3, commands/worktask.md:1026-1034, state-ledger.md:107-122. Draws the distinction the case turns on - the contract ships, the runner does not: \"The plugin's contribution is metadata plumbing (PL0 populates fields) + documentation/anti-patterns, not a shipped run-stage.sh\", confirmed by a grep of skills/worktask/scripts/. Risks name the FN-gate race (a headless DV dispatch pushing before the checkpoint) and the documented ban on dangerously_skip_permissions for PL/SR/FN. Resists the tier trap explicitly - canon \"calls out treating 'a CI login' as one's own escalation trigger as the wrong read\" - and stays standard. Leaks orchestration narration throughout (\"waiting on the Explore agent's findings\") and appends an epilogue after the plan.","split":"dev","type":"feature","grounding":"buried","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":157,"verdict":"defer","note":"Turns on a repo fact not visible from the trace. Reads \"our review tool\" as the eval review page and grounds it verbatim - evals/labelling.md says \"sample-for-labelling.py --out writes only a bare id list, so this richer file is written by hand\", and the --only ids.json is \"the case_ids field of the -sample.json\" extracted by hand on every rebuild. Two files, hand-written every run, quoted. The designated surface is skills/worktask/references/conductor-attachments.md, never opened. Settle it by checking whether conductor-attachments.md also describes two hand-authored review-tool inputs; if it does the request is ambiguous and the case under-specified, if it does not this is a plain wrong-surface miss. Plan itself is clean: S / 5, P2 \"none\", schema-drift risk against the committed 0.2.0/0.3.0 sample.json tranches.","split":"dev","type":"feature","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":165,"verdict":"pass","note":"Quotes the untested rule from the chain commands/appstore.md -> agents/release-engineer.md (\"Both apple and android markers present (a KMP repo), or neither -> stop and report. Never guess.\") and confirms the gap with grep -rl \"appstore|release-engineer\" tests/ returning nothing but COVERAGE.md's own listing. Then narrows what \"prove\" can mean here: the bats suite tests deterministic scripts, not live agent judgment, so P0 is a prose-drift contract test and the real behavioral eval is named as deferred against evals/README.md's taxonomy-gate moratorium (~100 label rows needed, currently 7) rather than silently dropped. Concedes its own P0's limit in the risks: \"Contract test proves the doc says the right thing, not that the agent obeys it - an LLM can still ignore correct prose.\" S / 9.","split":"dev","type":"feature","grounding":"obvious","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":183,"verdict":"pass","note":"Searches before answering and reports the search: grep -ri stripe \"hits exactly one line, a doc-reference URL example in skills/shared/test-selection-syntax.md:298\", no webhook handler, no payment code, nothing in .context/. Calls the no-surface branch, renders no template. Asks which repo the handler lives in, and keeps the security signal rather than dropping it with the plan - pre-commits the tier (\"squarely a --secure tier request (names a payment surface, retry-ordering/idempotency bug)\") and shows the exact trigger it will run once grounded, \"but I want that grounded in the real webhook handler, not guessed.\"","split":"dev","type":"bug","grounding":"absent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":191,"verdict":"pass","note":"Harness failed all six structural criteria because no template was rendered, but not rendering one is the correct move here and the case's own grounding says so. The designated surface is skills/incident-response/references/templates.md, and the response reads the answer straight out of it: \"Resolution Notice\" at :47-67, header \"## Incident Resolved: [Brief Description]\", fields Duration, Impact, Root Cause, Resolution, Verification, Follow-up. Then proves it is live, not orphaned - SKILL.md:203 points at it, agents/incident-responder.md:121 quotes it (\"resolution notice: ... adds root cause, verification, and the post-mortem date\"). Invokes the narrow exception, no /worktask line. Identical behavior to trace 60, which passed; the difference is that case is labelled expected=refute and this one expected=plan. Looks like a case mislabel, not a response failure.","split":"dev","type":"docs","grounding":"adjacent","route":"std","harness_status":"fail","harness_failed":["template-sections-present","scope-names-in-and-out","three-phase-model","effort-sized-with-complexity","single-worktask-trigger","routes-to-standard-tier"]}
{"case_id":193,"verdict":"fail","note":"Right skill, wrong file. Grounds in skills/task-folder-organization/SKILL.md - four allowed subdirs (designs/, images/, errors/, logs/), each with owner, naming grammar and consumer - and confirms no video path exists anywhere, with record_sim_video, adb shell screenrecord and Playwright video named as the three sources that would feed one. But the designated surface is that skill's references/examples.md, never opened, and P0 edits only \"the layout tree and subdir table in task-folder-organization/SKILL.md\" - adding a fifth subdir on one side of a pair that has to stay in sync. Otherwise disciplined: git policy stated up front rather than left implicit \"once someone commits a 40MB .mov\", filename grammar kept extension-agnostic across .mov/.mp4/.webm. XS-S / 7.","split":"dev","type":"feature","grounding":"adjacent","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":196,"verdict":"fail","note":"Misses the monitoring doc and then proposes monitoring. Builds a solid triage from stale-check.sh, resume.md's Live-agent rows and workflow-engineer.md's Cause 1 (agent gone, ledger honest) vs Cause 2 (all three enforcement layers L1/L2/L3 failed), and correctly orders classification first because manual state-merge.sh replay over a still-live agent \"fabricates a settled ledger under running work\". But the designated surface is skills/agent-coordination/references/hook-monitoring.md, never opened - so P1 proposes wiring stale-check.sh into a /loop or /schedule with a PushNotification on exit 1 without consulting the repo's own hook-monitoring reference, which trace 137 shows exists and is quotable at :186. S-M / 5-8, --emergency correct.","split":"dev","type":"incident","grounding":"buried","route":"emerg","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":201,"verdict":"pass","note":"Converts a doc-gap request into a named live defect. Quotes the grounded workspace-modes.md (\"The missing predicate is assignment... That is what dv-tree-preflight.sh asserts and nothing else does\"), then finds two cracks: worktask/SKILL.md step 4.8 resolves state.metadata?.workspace_path ?? full.metadata.workspace_path, so the top-level path beats a per-task one and TL-split DV1/DV2 resolve to the wrong tree; and the DV1/DV2 seed in initialization-patterns.md stamps stage, agent, model, description, context_refs, plan_file, worktask_id, priority and no workspace_path at all. Refuses the adjacent tracked debt by name - the base_ref ladder duplicated with branch-lib.sh, \"do not widen the divergence here\". Risks lead with blast radius: step 4.8 is DV's blocking pre-edit gate, so a wrong change false-blocks every DV run. M / 12, P2 \"none\" with a reason.","split":"dev","type":"migration","grounding":"buried","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":205,"verdict":"pass","note":"\"Premise doesn't hold\", with receipts: scan-secrets.sh:293-298 already checks command -v gitleaks and falls back to a regex scan, present since 01c6f6f (2026-06-29), covered by scan-secrets.bats:46-171. Adds the case the user may actually be hitting - the BSD grep engine-crash trap logged as F-37 in benchmark/results/ttt-multiplatform-findings-2026-08-15.md, fixed in #299 (db7b30f) to discriminate exit >1 and fail closed at exit 2 - and reads the behavior back the other way: \"no silent dead-stop. Script arguably does the opposite - fails loud on engine crash, by design.\" Asks for a repro instead of planning, and names the trap in the third option: softening fail-closed \"would be a regression against F-37, worth flagging explicitly if so.\"","split":"dev","type":"bug","grounding":"obvious","route":"secure","harness_status":"pass","harness_failed":[]}
{"case_id":213,"verdict":"fail","note":"Never looks in tests/. The designated surface is tests/shell/skills/agent-coordination__dispatch-depth.bats - a file whose name is the check the request says nobody runs - and the response proposes building that checker from scratch instead (\"ship the consistency-check script with a test fixture proving it catches a reintroduced mismatch\"). What it does find is real and specific: CHANGELOG-3.x.md:537 records the cap drifting to a stale 5 \"in seven places\", the canonical table is now titled \"Two independent ceilings\" (agent-coordination/SKILL.md:331) after CC 2.1.224, and commands/appstore.md:53 still cites the dead \"Three independent ceilings\" anchor while agents/developer.md:486 was updated. S / 9, standard tier.","split":"dev","type":"bug","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":214,"verdict":"pass","note":"Lands on the designated audit-lib.bats and quotes its line 4 as the match - \"the header contract every shared library in this plugin owes (anti-execution guard, include guard, no readonly, no load-time side effect)\" - noting it is \"the exact phrase the request paraphrases\". Then finds four separate classes of drift: README.md:9 says hooks/lib/model-switch-lib.sh while :18 and disk say hooks/model-switch-lib.sh; the three checkers implement one contract in three shapes (audit-lib H1-H4, state-read-lib 3 tests merging two and dropping the cwd check, corpflow-base unnumbered and adding a \"guard must be first executable statement\" check the others lack); an actual code violation at hooks/model-switch-lib.sh:349 using readonly -f against the README's absolute rule; and five library-shaped files carrying the same idiom with zero contract tests, \"so the 'every shared library' claim the checker itself makes is already false in practice\". Separates the @description tag as a different contract feeding capability-registry.sh. Leaks a capture-harness path into the deliverable (/private/var/folders/.../eval-capture-a3xygdvn/tree/...). M / 11.","split":"test","type":"refactor","grounding":"buried","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":215,"verdict":"fail","note":"Plans a lint over the symptom instead of the shared reader. Finds real drift with file:line - worktask_id // \"\" at branch-lib.sh:104 against // \"unknown\" in 13 sites, run_index // empty at state-patch.sh:1433 and // \"unknown\" at hooks/precompact-checkpoint.sh:60 against // 0 in 15 sites - then proposes a new ledger-fallback-lint.sh scanning 26 scattered jq reads, wired into the AD-3 lint job. Never opens the designated tests/shell/skills/state-read-lib.bats, or the shared state-read library it pins, which is where a single fallback default would live rather than be grepped for across the tree. S / 8.","split":"dev","type":"feature","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":216,"verdict":"pass","note":"Harness failed all six structural criteria for rendering no template, but the response reached the case's own designated surface and read the answer out of it. Cites stage-contracts.md 268-299 for the Sweep obligation matrix (13 codes, an exceptions table naming PL/FN/ST/IR, the other 9 taking the default, and a new code joining \"by saying nothing here\"), then tests/shell/skills/elicitation-sweep-contracts.bats - the grounding file - where AC-1 asserts the matrix stage list equals the canonical stage-codes list, and \"the AC-1 twin proves the check isn't vacuous: deleting one stage from the matrix's 'takes the default' line makes the completeness check fail\". Concludes \"nothing records which stages owe one\" is false at HEAD, offers to dig into a named stage if a specific artifact is missing its anchor, and gives no /worktask line. Same mislabel shape as 191.","split":"test","type":"docs","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["template-sections-present","scope-names-in-and-out","three-phase-model","effort-sized-with-complexity","single-worktask-trigger","routes-to-standard-tier"]}
{"case_id":218,"verdict":"fail","note":"Asserts the gap without checking the file named for it. Grounds in skills/shared/testing-strategy.md's Test-Execution Authority matrix and the hardcoded stage literals at hooks/test-execution-gate.sh:1160-1161, and correctly spots that the existing self-test parity lock (test-execution-gate-selftest.sh:278-286) is commented as \"a parity lock... not a bug catch\". But the designated surface is tests/shell/skills/test-authority-matrix.bats, never opened, so the claim \"nothing today would catch a table edit in testing-strategy.md ... that isn't mirrored in the script\" goes untested against the file whose name is that check. S / 6.","split":"dev","type":"feature","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":219,"verdict":"pass","note":"Corrects the premise, then finds the sharper bug underneath it. Reaches the designated tests/shell/meta/test-selection.bats and its matrix.tsv (grown 24 rows at #273 to 42 today), reproduces M2's logic against git ls-files and reports it green: \"the premise in the request is stale: M2 already asserts every glob matches a tracked path\". The real gap is narrower - 7 of 42 rows bundle paths via | alternation (R08 lists 9 agent files, R17 lists 12 shared docs), and M2 asks only whether the row matches something, so \"a renamed or deleted file inside one of those bundles (e.g. agents/technical-writer.md in R08) would currently pass M2 silently as long as a sibling in the same row still resolves\". Argues per-alternative is the file's own documented semantics, not a new rule. Defers the row-split to P1 only if the stricter check finds a stale alternative. XS-S / 4.","split":"test","type":"refactor","grounding":"buried","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":220,"verdict":"pass","note":"Harness failed all six structural criteria; the response instead reached the designated tests/shell/worktask/branch-lib.bats and ran it. T1 pins double-sourcing under set -euo pipefail (line 32), T2 pins all 16 symbols via type -t (line 42), T3 pins that the file sources nothing and declares no readonly; committed at c034415 and picked up by run-tests.sh's find over tests/shell/**. \"I ran all 67 tests in that file against the current branch-lib.sh (vendored bats, no network) - everything passes, T1-T3 included.\" Reconciles the count rather than waving at it: the header enumerates 18 symbols, and \"the request's count of 'sixteen' matches the public function surface exactly\", with BRANCH_TYPES (data) and _base_ref_ranked (private) deliberately outside the pin. No template, no trigger. Same mislabel shape as 191 and 216, and the only one that executed the evidence.","split":"dev","type":"bug","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["template-sections-present","scope-names-in-and-out","three-phase-model","effort-sized-with-complexity","single-worktask-trigger","routes-to-standard-tier"]}
{"case_id":221,"verdict":"pass","note":"Matches the request's own phrasing back to the grounded file: \"tests/shell/hooks/dedupe-lib.bats even labels the pair 'the PreToolUse gate and its PostToolUse companion'\". Quotes dedupe-lib.sh's header for the failure being guarded - \"two hooks deriving the key independently would orphan every marker the moment either drifted, disabling suppression silently\" - and states the actual gap precisely: one definition exists repo-wide today by grep, but nothing would fail if either hook grew a local copy. P0 pairs a static single-definition check with a declare -f identity comparison across test-execution-promote.sh's --lib-only sourcing path. Keeps two lookalikes out by name: the audit-dedup.sh JSON field-name literal (P1) and dv-screenshot-gate.sh's within-one-file duplicate jq at :128 and :182 (\"same pattern, different owner\"). XS / 7.","split":"test","type":"feature","grounding":"buried","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":224,"verdict":"pass","note":"Finds the asymmetry, not just the symptom. Quotes stale-check.sh:20-25 (\"Detection keys on agent liveness, never on elapsed time, so a legitimately slow stage is never reported stale\"), then shows the rule is mechanically enforced on one path only - state-patch.sh --task-replay refuses at exit 4 when the target is live, parked or indeterminate (:507-534) - while the automatic reattach path \"accepts any of pending|in_progress|completed|blocked|skipped unconditionally\" (:1176). Names the consequence: the healthy-long-running case is \"designed for but only enforced on the human-invoked replay path, not the automatic one\", with stale-check.sh itself \"opt-in per session\" per workflow-engineer.md:139. Fix is scoped to avoid regressing the ordinary transition - gate on current_status == \"in_progress\" AND requested_status == \"in_progress\" - and inherits the fail-safe so indeterminate liveness is never read as gone. M / 11.","split":"test","type":"bug","grounding":"buried","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":226,"verdict":"pass","note":"Harness failed all six structural criteria; the response refutes on the case's own designated surface. Names three places the behavior is already written down - SKILL.md's \"Slug-length canon: 50 characters\" with the word-boundary rule and its \"one whole word always survives\" exception, references/implementations.md's pseudocode for capSlug() and the full PRIORITY_ORDER map (P0-P4 to 0-4, unlabeled to 99), and the script's own header for _slug_cap and _priority_score_for_label with --self-test pinning 30 checks - then the grounded tests/shell/skills/milestone-helpers.bats cross-checking it against branch-lib.sh's derive_slug. Dates it to a34bab5 (2026-08-21) and notes a prior SKILL.md/implementations.md drift on the slug-length constant was already resolved. No template, no trigger, and offers to re-scope if the real gap is discoverability or a doc/behavior disagreement. Fourth of four all-six-criteria failures that is a case mislabel, not a response failure.","split":"test","type":"docs","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["template-sections-present","scope-names-in-and-out","three-phase-model","effort-sized-with-complexity","single-worktask-trigger","routes-to-standard-tier"]}
{"case_id":228,"verdict":"pass","note":"Runs the designated file rather than reasoning about it: \"I ran the suite with the vendored bats binary - all 13 pass - and confirmed run-tests.sh discovers it via find tests/shell -name '*.bats'\", so \"enforced nowhere a reader can run\" is stale for most of the grammar. Then isolates the true remainder: of four author-rule categories in plugin-root-resolution.md, three are contract-tested by plugin-root-refs.bats and corpflow-base.bats, and only the prose-paths phrasing rule has no checker - used in exactly one file (skills/code-comment-standard/SKILL.md), so \"low blast radius today but no guard against it growing wrong\". Requires the new checker be falsifiable, one clean case and one violation. Risk notes the phrasing is loose prose, so match structure rather than exact string. XS-S / 3-5.","split":"dev","type":"refactor","grounding":"buried","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":229,"verdict":"fail","note":"Searched tests/ and took the neighbouring file. Reads tests/shell/skills/cross-plugin-refs.bats and characterises it accurately - it \"verifies referenced Task(<plugin>:<agent>)/<plugin>:<command> targets exist as files in sibling repos, but not that the calling agent holds the grants those targets need\" - but never opens the designated tests/shell/skills/skill-refs.bats, and then asserts \"no validate-agent/lint-agent/grant-check script exists anywhere\". Evidence it does find is strong and live: agents/security-reviewer.md grants a narrow Bash allowlist at :9 while :202 mandates an always-on scan-secrets.sh run with no matching Bash(...) entry; agents/designer.md grants only Read, Glob, Grep, Write at :13 while :88-90 calls ToolSearch and mcp__pencil__* tools; CHANGELOG-3.x.md:450 records the same class at scale, 36 stray XcodeBuildMCP grants stripped from three agents. Notes prompt-audit and optimize-agent audit only over-broad grants, never missing ones. M / 12. Leaks deliberation into the deliverable (\"No action needed - I'll get an automatic notification when the Explore agent finishes; I don't need to poll or schedule a wakeup\").","split":"test","type":"bug","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":230,"verdict":"fail","note":"Guesses the file set the grounding file would have told it. The Goal opens \"Confirm which five files actually constitute this repo's planning-rules canon\" - the response does not know which five, picks a plausible list, and never opens the designated tests/shell/skills/request-plan-contracts.bats. What it finds inside its guessed set is sharp: estimation-methodology/SKILL.md carries two different scales both called \"5-factor complexity\", a 0-25 used by /estimate and plan-template.md and a 0-50 used by PL0 Stage-Set & Test-Mode, and the two band sets do not share cut points, so \"a score of, say, 20 lands in 'Medium' stage-set but the coarser '11-25 to scoped' test_mode band - possibly intentional, possibly drift\". Requires each proposed fix to state which reading it assumes before any file is edited. S / 7.","split":"dev","type":"feature","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":231,"verdict":"fail","note":"Never consults the guards file the case designates. Traces the mechanism correctly on its own: run-e2e.sh sets REPO_ROOT from SCRIPT_DIR/../.. and cds there before any write, so from a copied folder it mkdir -ps .context/images/, .context/designs/ and .context/logs/ under whatever sits two levels above the copy and appends to that audit.jsonl, while the ${REPO_ROOT}/skills/dv-screenshot-capture/scripts/apple-canvas.sh call fails non-fatally because it is wrapped in set +e and asserted on audit rows rather than exit code. Notes the existing README \"What gets mutated\" section at :53-66 covers only the in-place case. But tests/shell/benchmark/canvas-e2e-guards.bats is never opened, so what the guards already assert about copied-root runs goes unchecked. P0 does require running the fixture from a copy rather than documenting from a read. XS-S / 7.","split":"test","type":"docs","grounding":"buried","route":"std","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":234,"verdict":"pass","note":"Diagnoses burial rather than absence, which is what the request actually describes. Lands on the designated initialization-patterns.md:8-14 and confirms both halves live there - the seeding trigger and the re-run-aware run_index (\"next free planning index N... 0 on a fresh .context/\"). Then shows why a new maintainer never reaches it: \"grep for skills/worktask/references in README.md returns zero hits\", and the file is cited only laterally from peer implementation docs, \"none of them an onboarding entry point\". Verifies the negatives too - no CONTRIBUTING, ARCHITECTURE or ONBOARDING file, no docs/ directory, no ADR folder. Recommends consolidating into the file that already owns the section plus one README link near :288, \"avoiding a new file while fixing both the fragmentation and the burial problem\". Risk names a hazard specific to this repo: pl0-procedure.md and handoff-protocol.md \"are also runtime agent instructions, not just human docs\", so wording edits there could alter agent behavior - additive cross-references only. XS-S / 4.","split":"test","type":"docs","grounding":"buried","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":236,"verdict":"pass","note":"Names the helper, its one caller, and the coupling that makes the rule load-bearing: generatePreviewBlock at PreviewEnsurer.swift:298, called only by ensureFile at :395, consuming args tuples built in extractParameters where the comment at :157 records that a nil label means emit positional with no label. Confirms the negative by search - \"single caller\" / \"only caller\" returns nothing repo-wide. Sizes the work to match: one doc comment at the declaration, because jump-to-definition from the call site surfaces it there. Scopes out the access-level change as enforcement rather than the comment-only ask, and excludes two dv-screenshot-capture/templates/ files carrying a different single-permitted-location rule as \"unrelated helper, mentioned only because it surfaced in the initial search\". Risks catch the one real ambiguity - \"where its only caller can see it\" could mean the call site - and cover it with a cheap P1 rather than a guess. XS / 2.","split":"test","type":"feature","grounding":"buried","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":238,"verdict":"pass","note":"Names three fail-open paths that all write nothing and exit 0, and refuses to pick one blind. hooks/agent-stop.sh is the sole writer of stage_completion_hook rows and is wired through PL/FN/ST agents' own frontmatter hooks: field rather than plugin.json; CHANGELOG-3.x.md:546 documents that frontmatter-declared hooks silently skip when workspace trust has not been accepted, and :29-32 and :78-81 add missing jq and a symlinked audit.jsonl. Refuses the easy causal story: Step 6.5 patches state.json independently of this hook, \"so a hang implies something beyond the missing row is also stuck - worth confirming rather than assuming\". Cites the repo's own canon against its own clean tree: \"per estimation-methodology section A quiet local tree is not evidence, that doesn't rule out the reported failure in the environment where the pipeline is actually wedged\". Bars hand-editing state.json in favour of resume.md. S-M / 12, --emergency.","split":"dev","type":"incident","grounding":"buried","route":"emerg","harness_status":"pass","harness_failed":[]}
{"case_id":239,"verdict":"pass","note":"Finds two concrete lost-update windows inside a lock that exists to prevent exactly this. state-patch.sh's mkdir-spinlock (:902-943) is documented so \"concurrent sibling writers cannot drop a patch\", but _lock_acquire on timeout (STATE_LOCK_TIMEOUT_S, default 5s) \"returns 1 and the caller proceeds unlocked + WARN\" (:601-602, 627-653) - a read-apply-rename against a stale pre-lock snapshot - and the idempotency check at :1479-1484 \"reads state.json before any lock is taken, so its verdict can be based on a snapshot a sibling write is about to invalidate\". Explains the second half of the symptom rather than only the first: a lost patch leaves state.json durably wrong, and resume.md's ledger-shape table reads the same corrupted ledger every reattach, \"so the same failure recurs deterministically rather than being a one-off\". Risks recognise the escape hatch is load-bearing - removing it \"needs a bounded retry/backoff instead of an unbounded wait, or the fix could itself introduce a hang\". Repro forces the race (drive the timeout to 0, or hold the lock) rather than waiting on timing luck. M / 16, --emergency.","split":"test","type":"incident","grounding":"buried","route":"emerg","harness_status":"pass","harness_failed":[]}
{"case_id":240,"verdict":"pass","note":"Corrects the mental model embedded in the request instead of designing to it: \"'Which contract it thinks is broken' is worth naming directly: the gate has no concept of contract - it never parses WHY-vs-WHAT-vs-history semantics. It's a pure line-density/run-length heuristic.\" Two live hypotheses, both mechanical: the block string is written into the subagent's hookSpecificOutput.additionalContext and may be swallowed at the subagent boundary so the user sees only a blocked-and-retried agent, or BLOCK_MAX=3 is miscalibrated against multi-line JSDoc/docstring conventions with @param/@returns that would score as essay on every function. Risks add a third candidate, the agent_type string match (*developer*|*code-fixer*|*test-generator*) catching an agent it should not. States it cannot reproduce here - this worktree's audit log has zero comment_density_block rows because no developer agent has run - and blocks the tempting wrong fix by name: \"fix the gate or the standard - not silence the gate\". S / 11, --emergency.","split":"dev","type":"incident","grounding":"buried","route":"emerg","harness_status":"pass","harness_failed":[]}
{"case_id":241,"verdict":"fail","note":"Reads \"companion\" as the wrong pair. In this repo the phrase is a term of art for the test-execution gate and its PostToolUse companion - trace 221 quotes dedupe-lib.bats labelling them \"the PreToolUse gate and its PostToolUse companion\" - and the designated surface is tests/shell/hooks/test-execution-promote.bats, the companion that records a runner exit. The response instead reads it as the Python eval harness and plans a durable per-case failure record for evals/scripts/eval-capture.py's _sweep(), which today \"catches capture_case's RuntimeError per case, appends the id to an in-memory failures list, and only ever surfaces it via sys.stderr.write(...) and the process exit code\". The eval-side analysis is coherent on its own terms - eval-grade.py:96-99 does conflate never-attempted with attempted-and-failed in one missing_captures bucket - and it protects the never-fabricate-a-response contract by keeping the record out of responses/. Wrong surface all the same. S / 8, --emergency.","split":"test","type":"incident","grounding":"buried","route":"emerg","harness_status":"fail","harness_failed":["finds-the-real-surface"]}
{"case_id":244,"verdict":"pass","note":"Narrows the premise to the part that is actually missing. Finds the grounded commands/arch-review.md and reads --checklist back against the request: it already scores Overall Architecture, SOLID, Scalability, Security and Maintainability 0-10 and renders a Component | Current | At 10x Scale | Recommendation table, so \"that is 'grades our boundaries and how far they scale'\". The gap is repeatability - nothing persists scores across runs. Models the new Trend column on /arch-debt --report's existing table rather than inventing a shape. Risks catch the trap that would have silently voided the feature: \".context/ is gitignored and per-worktask ... so writing history there would silently lose it. Mitigation: pick a repo-tracked path in P0 before wiring anything\", and warn that unpinned 0-10 scores make trend deltas \"grading noise, not real drift\". Refuses a parallel command and refuses to invent scheduling when /loop exists. S / 9.","split":"test","type":"feature","grounding":"adjacent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":246,"verdict":"pass","note":"Calibrates a partial-existence case correctly - neither refuting nor swallowing the premise. \"/create-agent scaffolds new agent files from the canonical frontmatter template, and /optimize-agent --dry-run can audit one afterward. But nothing forces that check to run\": desc-lint.sh, the one deterministic frontmatter lint, covers only the description field (grammar G1-G5, length G6) and is \"CI / repo-maintenance helper only - never read by agents at runtime\", and /create-agent's Next Steps ends at \"review the file\". Verdict stated plainly: \"the complaint is accurate for the part that matters\". P2 then catches what P0/P1 do not - a sweep over all agents/*.md, \"the case the user actually named - copying an existing file, not just running the command\". Risk flags the duplicate-logic trap against desc-lint.sh with a concrete either/or. S / 9.","split":"test","type":"feature","grounding":"adjacent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":249,"verdict":"pass","note":"Surveys before asserting. Counts the drift rather than describing it: 28 files running 56 lines (request-plan.md) to 1042 (worktask.md); most end at \"## Integration\" while appstore.md ends at \"## Relationship to the pipeline\", megatask.md at \"## Relationship to /worktask and /milestone\", cc-update.md adds \"## Worktask Routing (BINDING)\" and \"## Edge Cases\", improve-yourself.md adds \"## Constraints (DO NOT)\" and \"## Error Handling\"; and \"only 7 of 28 files carry a version: field\", all seven named. Finds the grounded commands/optimize-command.md and places it correctly - the audit tooling ships, the cleanup does not - so this \"isn't a gap to build\". P0 separates the judgment from the mechanism: \"can't mechanically fix 28 files without a decided target shape first - that's the part --dry-run can't decide for you\", and defines two templates so a 1042-line orchestrator is not forced into a short-command shape. P1 keeps real outliers as a named slot \"instead of being silently dropped\". M / 12.","split":"test","type":"refactor","grounding":"adjacent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":250,"verdict":"fail","note":"Asks where the parallel case in this same run plans. Finds the surface and reads it correctly - the request \"matches /roadmap exactly (timeline + milestones + dependencies) - not /request-plan's effort-range format, which explicitly excludes roadmaps\" - then stops at \"No subject named. Can't plan without one - asking, not guessing.\" It even supplies the candidate it declines to use: recent activity is request-plan eval batching under #359, batch 6 pinned, 58 held-out cases. Trace 251 faces the same shape with a missing input (no capacity baseline) and instead states the assumption explicitly and plans against #359/#353. Shortest response in the set at ~457 chars.","split":"dev","type":"feature","grounding":"adjacent","route":"std","harness_status":"fail","harness_failed":["asked-instead-of-planning"]}
{"case_id":251,"verdict":"pass","note":"Grounds an allocation plan in the real backlog rather than a template. Quotes commands/sprint.md's own description back (\"Plan sprint with capacity analysis, task breakdown, and resource allocation\") with its --capacity/--from-backlog/--duration/--include-debt flags, then names the two actual open issues, #359 (planning-approved, complexity:high, updated 2026-09-05) and #353 (collapse the three divergent base-ref ladders onto _base_ref_ranked, per MEMORY.md's 4.0.29 entry). Catches what a tracker-only read would miss: \"the working tree also has 13 unstaged deletions under evals/ - uncommitted WIP tied to #359 that an allocation exercise shouldn't plan blind to\". Handles the missing input by declaring it instead of blocking - \"state the capacity assumption explicitly via /sprint --capacity <points> rather than letting the template guess one silently\" - and right-sizes the ceremony for a two-item backlog. Refuses to build new tooling. XS-S / 7.","split":"test","type":"feature","grounding":"adjacent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":254,"verdict":"pass","note":"Finds the reviewer already built, then finds when it fails to fire. Names the whole existing surface - corpflow:ethics-reviewer, /ethics-review, the claude-constitution skill, and the PL0 gate that inserts an ET0 checkpoint when a description scores >=5 on a weighted risk-keyword table - then locates the real gap in the trigger's timing: \"that gate scores the task description text at planning time, before any code exists. It has no signal from the actual diff\", so \"a change whose description is bland ('refactor session handling') but whose diff touches auth/payment/ranking code never gets gated\". Reuses the ET0 insertion pattern, state-patch.sh --task-block and the pass/conditional/block cascade instead of a parallel system. Risks require the new signal be additive-only against shared worktask canon, reuse the existing negative-indicator suppression to limit path false positives, and confirm via state-patch-selftest.sh that no coverage exists before extending the scoring silently. M / 14.","split":"test","type":"feature","grounding":"adjacent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":256,"verdict":"pass","note":"Parses \"accountable\" as ownership rather than capability, and shows why. agents/workflow-engineer.md (Stage Code WE) already carries both runbooks the request names - Manual Repair steps 1-5 with F4 corrupt-state.json recovery, and Stage Stuck at in_progress via stale-check.sh and resume.md verdicts - and the SubagentStop hook registration is confirmed by reading plugin.json. So \"the capability to repair a run-record and diagnose a stuck stage is fully built; the accountability - something or someone that actually watches and routes to it - is not\", evidenced by stale-check.sh being \"opt-in per session\" and a repo-wide grep finding no monitors manifest entry. Risk catches a subtle training effect: a standing check \"could train someone to read stale-check.sh exit 3 (liveness-undetermined) as 'healthy'\", so the added test should assert against it. Requires every new ownership mention to link back rather than restate the runbook. S / 7.","split":"test","type":"feature","grounding":"adjacent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":259,"verdict":"pass","note":"Separates two systems the request conflates, before planning either. version-bump-from-git.sh (the grounded file) is already deterministic and wired into release-engineer.md RE1 for the target repo; commands/cc-update.md's Plugin Version Bump Policy still decides corpflow's own plugin.json version by qualitative triggers, and \"the doc says outright: 'DV picks the tier'\", confirmed against history where \"every chore(release): Bump to X.Y.Z commit is a manual call, not a computed one\". States the assumption with a cheap exit - \"if you meant the RE-stage/target-repo path, say so and I'll re-scope - that one's done\" - rather than blocking on it. P0 backtests the rule instead of asserting it: re-run the script over 3-4 recent real ranges (the 4.0.2x bumps) \"and confirm the verdict matches what was actually shipped\". P2 closes the loophole the migration would otherwise open, gating --bump-min/--force behind a recorded --reason \"so a human overriding the deterministic verdict leaves a trace instead of quietly reintroducing prose judgment\". S / 9.","split":"test","type":"migration","grounding":"adjacent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":262,"verdict":"fail","note":"Planned where the case expects a question, and the missing input is the one only the user holds. Confirms \"no Microsoft Teams, MicrosoftTeams, or Office365 reference anywhere in the repo\", then assumes an incoming webhook via env var and plans rather than asking which channel, tenant or webhook. The grounding work underneath is strong: finds the existing conductor.PushNotification Stop hook at plugin.json:155-170 as the pattern and firing point, adopts publish-pl-issue.sh's shape (sanitize before content leaves the repo, non-blocking, one audit row per attempt with a reason enum, dedupe key), and establishes that gates are not a ledger status at all - there is no awaiting_approval enum, only PL0.metadata.plan_gate/fn_gate plus approval_received/fn_gate_waiting audit rows. Flags MessageCard deprecation and treats the webhook URL as a discovered rather than requested asset, correctly leaving the tier standard. Same behaviour-selection failure as trace 36. Emits a stray non-Latin character in the final trigger line.","split":"test","type":"feature","grounding":"absent","route":"std","harness_status":"fail","harness_failed":["should-have-asked-not-planned"]}
{"case_id":263,"verdict":"fail","note":"Commits to one reading of an ambiguous subject and plans, where the case expects a question. Resolves \"onboarding handbook for new reviewers\" to evals/labelling.md and defends it - the doc is \"literally titled 'Human labelling - the trace review page and the loop around it'\" - then largely refutes the staleness claim by checking it: keybindings, filter options, JSONL export shape and the label-align.py / sample-for-labelling.py flags \"all match current evals/scripts/*.py --help output exactly\", and the doc was last touched in 9280b39, the commit that pinned batch 6, \"so the labelling walkthrough itself is not stale\". Real drift found is one script: scan-contamination.py works and is documented only in CHANGELOG.md, absent from both README.md's script list and labelling.md. Never flags the competing reading (code or PR reviewers, worktask pipeline) the way trace 259 does with its two candidates. S / 4.","split":"dev","type":"docs","grounding":"absent","route":"std","harness_status":"fail","harness_failed":["should-have-asked-not-planned"]}
{"case_id":264,"verdict":"pass","note":"Reports the search space, not just the miss. No .context/ state or MEMORY.md entry, no nightly/cron/monday/digest logic by full-text grep, no schedule: or cron trigger in .github/workflows (only test.yml on push/PR), and it checks the live scheduler rather than the tree alone - \"no cron job is registered in this session (CronList -> 'No scheduled jobs')\". Renders no template and no trigger. The three candidates it offers are where the job would actually live: a scheduled agent from another session, a cron or CI workflow in a different repo, or an external scheduler posting the summary.","split":"test","type":"bug","grounding":"absent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":266,"verdict":"pass","note":"Names the rule and follows it exactly: grep for zendesk, help center/centre, helpdesk and support macro returns nothing, README confirms the repo is a worktask orchestration plugin, and \"per the skill's step 1 rule ('no surface at all -> ask, and only ask')\" it renders no template and no trigger. Two questions closed with \"Which is it?\", and the second covers the greenfield case the first would miss: if corpflow is meant to gain a help-centre feature, \"tell me that explicitly, since nothing here currently suggests it\".","split":"dev","type":"migration","grounding":"absent","route":"std","harness_status":"pass","harness_failed":[]}
{"case_id":267,"verdict":"pass","note":"Harness scored this as planning-instead-of-asking; the response plans nothing. No template, no /worktask line, and it states the choice outright - \"I'm asking rather than fabricating a plan against a system I can't see\". Search is reported precisely, including why each hit is noise: \"git-index staging in agents/project-manager.md, a SwiftUI 'deployment constraints' example in agents/software-architector.md, and develop/master branch-staging discussion in MEMORY.md/changelog history\", with the plugin's own IR tooling set aside as \"process for running an IR stage inside a worktask, not an actual cluster to bring back up\". Its two asks end in a colon and a period - \"Two things could be true - which is it:\", \"Let me know which\" - and the response contains no question mark anywhere. Same detector artifact as trace 43; every clarify case in this run that carries a \"?\" passed.","split":"test","type":"incident","grounding":"absent","route":"emerg","harness_status":"fail","harness_failed":["should-have-asked-not-planned"]}
{"case_id":269,"verdict":"pass","note":"Asks, and asks the question that decides whether this is a worktask at all. Reports the grep (paging/on-call/PagerDuty/Opsgenie) and places the only hits as generic \"notify on-call\" lines in incident-response/templates.md and release-engineering/references/rollback-template.md, concluding the repo \"doesn't own or call a paging service\". Second question separates a code fix from a live vendor outage and pre-commits the right answer for one branch: \"if it's pure ops with no code surface, this isn't a /worktask - I'd hand you a manual escalation checklist instead\". Closes \"No /worktask line yet - give me the repo/system and I'll come back with a grounded plan and trigger.\"","split":"test","type":"incident","grounding":"absent","route":"emerg","harness_status":"pass","harness_failed":[]}
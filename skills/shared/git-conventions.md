---
name: Git Conventions
---

# Git Conventions

## Commit Message Format

Based on [Conventional Commits 1.0.0](https://www.conventionalcommits.org/en/v1.0.0/).

```
#<issue> <type>[optional scope][!]: <short summary>  (max 72 chars)

[Optional body: explain WHY and context, separated by blank line]

[Optional footer(s): git trailer format]
```

Rules: present tense, capitalized summary, no ending period; prefix the issue code
(`#PROJ-123`) whenever one exists (§ No issue anchor covers the other case); a blank line
separates body from summary. Scope is optional context in parentheses
(`feat(auth): Add OAuth2 flow`). Footers use git trailer format (`token: value` or
`token #value`); hyphens replace spaces in tokens, except `BREAKING CHANGE`.

### Types

| Type     | Use for                                 | SemVer  |
|----------|-----------------------------------------|---------|
| feat     | New feature                             | MINOR   |
| fix      | Bug fix                                 | PATCH   |
| perf     | Performance improvement                 | PATCH   |
| refactor | Code restructuring (no behavior change) | —       |
| build    | Build system, dependencies              | —       |
| ci       | CI/CD configuration                     | —       |
| chore    | Non-functional cleanup                  | —       |
| docs     | Documentation only                      | —       |
| style    | Formatting, whitespace (no logic change)| —       |
| test     | Tests only                              | —       |

### Breaking Changes

Append `!` after type/scope for breaking API changes (SemVer MAJOR) —
`#PROJ-123 feat(api)!: Remove deprecated /v1 endpoints` — or use an uppercase
`BREAKING CHANGE: <what broke>` footer.

### Comment-character trap

The `#<issue>` prefix collides with git's comment character. Editor-driven invocations
(`git rebase --continue`, `git commit` with no `-m`, `git commit --amend`, conflict-resolution
commits) default to `strip` cleanup, which deletes every `#`-leading line: the subject vanishes
without a warning or non-zero exit, and the body's first paragraph takes its place.
`git commit -m`/`-F` default to `whitespace` cleanup and need no remedy.

#### Remedy — per invocation

```bash
git -c core.commentChar=auto rebase --continue   # auto: git picks an unused char
git -c core.commentChar=';'  commit --amend      # explicit char (git < 2.10)
git commit --cleanup=verbatim -F <message-file>  # final message; keep it byte-for-byte
```

`-c` scopes the override to one command. Don't write the setting to git config: config
mutation is off-limits, and it would change message handling for every other command in the
checkout.

## This spec outranks `git log`

Don't infer commit or PR style from repository history: existing commits are often
non-compliant (pre-convention, hand-written, or generated by a tool that did not read this file).
Read this document before the first commit of a session; where the log disagrees with it, follow
the spec and mention the discrepancy.

### No AI-attribution footers

A commit or PR body carries no `Generated with …` line and no `Co-Authored-By: Claude` trailer:
authorship is the human operator's, and the footer would propagate into release notes and blame.
Setting `attribution.sessionUrl` also omits the session link, enforcing the rule at the tooling
layer. The harness's attribution reminder yields to this rule (lines set by managed settings
still apply).

## No issue anchor

Not every change has an issue behind it. The `#<issue>` subject prefix and the `Closes #<N>`
pull-request trailer share two arms, selected by whether an issue anchor resolves, not by author
preference.

### What resolves an anchor

An issue anchor is what `resolve_issue` (`skills/worktask/scripts/fn-preflight-cmds.sh`) resolves,
first match wins: `metadata.github_issue_url` in the ledger, `.context/gh-issue.json`,
`metadata.github_issue_number`, then a `<type>/<NNN>-<slug>` branch or a `#NNN` in the last five
commits.

| | Anchor resolves | No anchor |
|---|---|---|
| Commit subject | `#368 fix(worktask): Close the trailer hole` | `fix(worktask): Close the trailer hole` — a bare conventional subject is compliant, not a degraded form |
| PR trailer | `Closes #368` required; `fn-preflight.sh validate-pr` blocks a body without it | Omitted. `validate-pr` records an audit-defer row and passes; `pr-body-lint.sh` P4 raises no finding |

### Never invent a number

Don't invent an issue number to satisfy either rule: it links the merge to an unrelated issue and
closes it. Omitting a trailer is always better than fabricating one.

## Git Safety (beyond CC defaults)

- Never push directly to main/master without PR
- Never delete branches without explicit user instruction

### Auto-mode Git Safety

In auto mode the runtime enforces these guards on its own:

| Guard | Behavior |
|---|---|
| Destructive git — `git reset --hard`, `git checkout -- .`, `git clean -fd`, `git stash drop` | Refused unless the prompt explicitly asks to discard those changes |
| `commit --amend` | Refused unless the agent made that commit this session — after a failed hook the commit did not happen, so amend would destroy prior work |
| IaC `destroy` | Bare `destroy` refused; the target stack must be named |
| `rm -rf` on an unresolvable variable | Prompts; catastrophic removals inside `$(…)`/backticks/`<(…)` prompt even under `--dangerously-skip-permissions` |
| Session transcript files | Tampering blocked |
| `--force`, `--amend`, `--no-verify` on git/gh | No longer auto-approved by `/commit-push-pr` — they prompt |

#### Consequences for FN and unattended runs

- Denial reasons surface in the transcript, the denial toast, and `/permissions` recent denials.
  `autoMode.classifyAllShell` routes all shell commands through the classifier.
- FN-stage commit/cleanup is unaffected: no worktask flow amends a non-agent commit or expands
  an unresolved variable into an `rm` target.
- Under an unattended run (`--auto=[finalization]`, `/megatask`) a prompt on a dangerous flag is
  a stage failure to route through the retry matrix, not something to work around.
- Background sessions follow the repo's `CLAUDE.md`, so these rules apply there too.

## Pull Request Format

Title: `<type>[scope][!]: <summary>`

```markdown
## Motivation
[Why this change is needed]

## Changes
- [Change 1]

## Test plan
- [How this was verified]

## Visual evidence
[UI changes only — inserted verbatim; see § Visual evidence]

## Notes
[Additional context, testing instructions]

Closes #<N>
```

### Required elements

- `Test plan` is required: `fn-preflight.sh` blocks a body without that heading.
  `Closes #<N>` is required when an issue anchor resolves and omitted when none does
  (§ No issue anchor); `pr-body-lint.sh` P4 flags a missing closing keyword only in the first
  case. `Visual evidence` appears only on runs that captured screenshots.
- `Unresolved decisions` appears only when a run with no reachable human shipped escalate items
  undecided. `fn-preflight.sh unresolved-decisions` writes it at byte 0 of the body; don't write or
  edit it by hand. Each item is `- **sw-SR0-1** (SR0): <question>`, or the id alone when the
  question would not survive the sanitiser.
- Keep local paths out of a PR body: `.context/`, `/Users/…`, `~/…` and `../…` mean nothing to a
  reviewer and leak host layout. Code spans count too; `pr-body-lint.sh` checks them.

### Visual evidence

Both paths end in `attach-visual-evidence.sh --emit pr`. A worktask PR gets the block from FN
automatically on `metadata.requires_screenshots: true` (the default). An ad-hoc PR
(`gh pr create` outside a worktask) gets it from:

```bash
bash skills/worktask/scripts/adhoc-visual-evidence.sh --emit pr
```

Run it every time (it gates itself) and splice its stdout between `## Test plan` and `## Notes`
before `gh pr create`, so no second API call patches the body. Empty stdout means insert nothing.

#### Capture conditions

It captures only when the diff touches the UI path classes owned by
`detect-ui-change.sh --path-classes`, refuses to run in a worktask tree, and exits 0 on every
failure so a missing screenshot never blocks the PR. `ADHOC_SKIP=1` suppresses a run
the heuristic gets wrong; `--base <ref>` overrides the resolved target.

## Merge Strategy

Integrate PRs with a merge commit, not a squash or rebase-merge.

```bash
gh pr merge <number> --merge
```

Don't use `--squash` or `--rebase`: they collapse or rewrite the branch history carrying
per-stage worktask context, and the commit boundaries (one per stage or logical change) must
survive on the target branch. Keep the auto-generated merge subject
(`Merge pull request #N from <branch>`); the PR title/body carries the summary.

## Branch Naming

Canonical source for worktask branch grammar; other files point here instead of restating it.

```
<type>/[<ticket>-]<slug>
```

The ticket segment is optional: `bugfix/ov-156-reconstruction-scan-flow` and
`feature/add-dark-mode` are both conventional, and `branch_is_conventional` accepts both, so an
existing ticket-less branch is never churned.

### Ticket derivation

The ticket comes from the goal text only: the first `\b[A-Z]{2,}-\d+\b` token, lowercased
(`derive_ticket` in `skills/worktask/scripts/branch-lib.sh`). There is no issue lookup, since the
branch is named before any issue-linked commit exists; a goal without such a key gives the
ticket-less shape. The key is stripped from the slug body so it appears once, and its length
counts inside the 48-character cap (§ Slug budget).

The ticket segment does not replace the PR closing keyword: the segment locates the work on every
ref, log line and PR URL, the keyword closes the issue on merge. Emit both when the goal supplies
a key.

### Slug budget

`<ticket>-<slug>` fits within 48 characters. Truncation drops the trailing partial segment
rather than cutting mid-word: `bugfix/ov-164-…-blinking-before`, not `…-blinking-before-r`.
At least one whole word always survives, even one longer than the remaining budget, so a long
issue key can never starve the slug to nothing.

### Batch (`/megatask`) branches — one convention, two entry points

Milestone/batch runs name branches through
`skills/shared/milestone-helpers/scripts/milestone-helpers.sh branch-name`, not
`branch-lib.sh`, shape `<type>/{issue#}-{slug}`. The two entry points compare as follows:

| | worktask (`branch-lib.sh`) | batch (`milestone-helpers.sh`) |
|---|---|---|
| Type | `derive_type` on the goal | `derive_type` on the issue title — same function, sourced |
| Ticket segment | optional, from the goal text | always the issue number |
| Slug budget | 48, shared with the ticket segment | 50, issue number sits outside it |

#### One rule, not two

The budgets differ deliberately; everything else is shared. `milestone-helpers.sh` sources
`branch-lib.sh` for `derive_type` and `BRANCH_TYPES` rather than keeping its own copy, and an
unreachable library stops the run (exit 2) instead of falling back to `feature/`. Kebab body,
trimming and whole-word truncation follow § Slug budget, pinned by cross-check tests in
`tests/shell/skills/milestone-helpers.bats`: the same title yields the same type and slug body
from both entry points.

### Type vocabulary

13 tokens, accepted by the already-conventional check: `feat`, `feature`, `bugfix`, `hotfix`,
`refactor`, `perf`, `docs`, `chore`, `test`, `ci`, `build`, `style`, `revert`. There is no `fix`:
a pre-existing `fix/<slug>` branch is non-conventional and is renamed onto the derived
`bugfix/`/`hotfix/` target. Generation emits 12 of the 13: new work defaults to the long form
`feature`, and a title whose leading word is "Build" emits `feat`. `style` is accepted but never
generated, and any `feat` branch is accepted so a short-form branch is never churned. The single
machine-readable copy is `BRANCH_TYPES` in `skills/worktask/scripts/branch-lib.sh`; both
`derive_type` and `branch_is_conventional` derive from it. Branch-only: don't add `feature` to
the commit-type table above.

#### Type derivation

`derive_type` matches `fix` as a word (start, end, or punctuation-bounded): "…and fix." is
a bug report, `prefix`/`fixture` are not. It also recognises the defect vocabulary `blink`,
`flicker`, `glitch`, `broken`, `regression`, `incorrect`, `wrong`, `fails`, `failing` for
reports that never use the word "fix". `hotfix` is tested first, since it contains `fix`.

Arms are tried in order and the first match wins: `revert`, `hotfix`, the fix and defect words,
`refactor`, `perf`, `docs`, `test`, `ci`, a title whose first word is "Build" (`feat`), then
`build`/`packaging` and `chore`; an unmatched title defaults to `feature`. Earlier arms still win
over a leading "Build" ("Build fix for the cache" → `bugfix`, "Build the docs site" → `docs`),
and "build" in any other form or position (`build-time`, `builds`, `rebuild`) stays `build`.

### Conventionality is a predicate, never a judgement

The only authority is `branch_is_conventional()` in `skills/worktask/scripts/branch-lib.sh`
(queryable as `bash ${CLAUDE_PLUGIN_ROOT}/skills/worktask/scripts/branch-name.sh --check <name>`).
Don't decide by eye that a name looks conventional and skip the naming step: that is how a
`fix/<slug>` branch survived after `fix` left the vocabulary.

### Guard ladder (every arm is a no-op or a refusal, never a failure)

Every run returns two names: `branch=` is the local branch after it (final stdout line),
`target_branch=` is what the remote branch (the PR head) should carry. Blocking the local
rename never blocks the target.

| Guard | Local branch | `target_branch=` |
|---|---|---|
| Name already conventional | no-op — a deliberate name is never churned | empty — `branch=` is the answer |
| Upstream already tracked | no-op — a rename orphans the remote ref | derived |
| On the integration branch | refuses | empty — never a PR head |
| Target name already exists | no-op | derived |
| Host workspace (linked worktree) | renamed (default) — `BRANCH_NAME_WORKTREE_RENAME=0` keeps the host's name | derived |
| Detached HEAD / not a repo | skipped | empty |

#### The linked-worktree arm

The branch is renamed like any other checkout, so the local name and the PR head agree. The
host's branch↔workspace mapping is updated rather than preserved: a host-assigned placeholder is
not worth keeping, and a host may rename the branch mid-run without telling the pipeline. `BRANCH_NAME_WORKTREE_RENAME=0` restores defer-to-host
(`workspace-modes.md § Host mapping — updated, not preserved`).

### Once-only rule

The branch is named once, at the start of the planning stage
(`skills/worktask/scripts/branch-name.sh`), before any commit exists, and the rename never
repeats. `facts.branch` on the run ledger records the planned name; finalization reads it from
there rather than re-deriving it from `git`. The planned name may be refined at most once per
run, after planning and before the plan is published, from the approved plan's `title:`: a
ledger-only write with no git mutation (`commands/worktask.md § Step A.4b`).

### Goal text becomes a public, durable branch name

The task description feeds `derive_slug` and survives (truncated, kebab-cased) into the branch
name, an `audit.jsonl` row and, once pushed, a remote ref and the pull-request URL, which on a
public repository is world-readable and effectively permanent. Keep credentials, secrets and
other sensitive values out of the task description; treat it like a commit message or PR title.

## GitHub Issue Types

| Type | Keywords                                  |
|------|-------------------------------------------|
| Bug           | Problems, errors, crashes        |
| Feature       | New functionality, enhancements  |
| Task          | Refactoring, technical debt      |
| Documentation | Missing or incorrect docs        |
| Configuration | Build system, CI/CD, setup       |

{
  "skill_name": "request-plan",
  "grading": "Binary code-checked assertions over the skill's rendered plan. Every assertion is objectively decidable from the text — no judge, no scale. Criteria needing interpretation are listed under `deferred` and are deliberately NOT graded until a validated evaluator exists.",
  "assertion_types": {
    "contains_all": "every literal substring must be present",
    "contains_none": "no listed literal substring may be present",
    "regex_all": "every pattern must match (multiline)",
    "regex_any": "at least one pattern must match (multiline)"
  },
  "shared_assertions": [
    {
      "id": "template-sections-present",
      "why": "references/plan-template.md fixes the section order; a dropped header breaks the contract",
      "type": "contains_all",
      "values": [
        "## Context",
        "## Goal",
        "## Scope",
        "## Phases",
        "## Effort",
        "## Risks"
      ]
    },
    {
      "id": "scope-names-in-and-out",
      "why": "Out-of-scope is what stops scope creep; a plan with only In is half a scope",
      "type": "contains_all",
      "values": [
        "**In:**",
        "**Out:**"
      ]
    },
    {
      "id": "three-phase-model",
      "why": "P0/P1/P2 per skills/shared/three-stage-planning.md",
      "type": "regex_all",
      "values": [
        "P0\\s*—\\s*Required",
        "P1\\s*—\\s*Nice-to-have",
        "P2\\s*—\\s*v1\\.1"
      ]
    },
    {
      "id": "effort-sized-with-complexity",
      "why": "Effort is a T-shirt size AND a 0-25 complexity score together; a bare digit anywhere in the text is not evidence either was given. An approximation marker on the score still answers the question — rejecting `~8` would fail a correct plan for hedging",
      "type": "regex_any",
      "values": [
        "\\|\\s*(XS|S|M|L|XL)(\\s*[-—–]\\s*(XS|S|M|L|XL))?\\s*\\|\\s*[~≈]?\\s*([0-9]|1[0-9]|2[0-5])\\s*(/\\s*25)?\\s*\\|",
        "(XS|S|M|L|XL)[^\\n]*\\b([0-9]|1[0-9]|2[0-5])\\s*/\\s*25\\b"
      ]
    },
    {
      "id": "single-worktask-trigger",
      "why": "The handoff is one ready-to-paste command line",
      "type": "regex_all",
      "values": [
        "/worktask(\\s+--\\w+)?\\s+\""
      ]
    },
    {
      "id": "no-separate-test-phase",
      "why": "Tests live inside each phase's scope, never as a phase of their own",
      "type": "contains_none",
      "values": [
        "P1 — Testing",
        "P2 — Testing",
        "Phase: Testing"
      ]
    }
  ],
  "evals": [
    {
      "id": 1,
      "prompt": "the test-execution gate keeps denying QA test reruns even after i've edited files in the tree. help me plan a fix before i dive in.",
      "assertions": [
        {
          "id": "names-the-gate-surface",
          "why": "A grounded plan must name the actual failure surface, not restate the request",
          "type": "regex_any",
          "values": [
            "test-execution-gate",
            "CORPFLOW_TEST_DEDUPE",
            "testing-strategy",
            "dedupe"
          ]
        },
        {
          "id": "plans-a-rerun-path",
          "why": "The complaint is that a rerun is refused; a plan that never addresses re-running after a tree change has not answered the request",
          "type": "regex_any",
          "values": [
            "rerun",
            "re-run",
            "tree",
            "hash",
            "changed"
          ]
        }
      ],
      "deferred": [
        "Whether the Context section reflects THIS repo rather than a generic template — needs a validated judge."
      ],
      "grounding": [
        "hooks/test-execution-gate.sh",
        "skills/shared/testing-strategy.md"
      ]
    },
    {
      "id": 2,
      "prompt": "i want /estimate --quick to support --export csv too, not just --detailed. what's the plan?",
      "assertions": [
        {
          "id": "reuses-existing-export-surface",
          "why": "The 13-file pack in csv-export-templates already exists; planning a second exporter is the failure this case targets",
          "type": "regex_any",
          "values": [
            "csv-export-templates",
            "13[- ]CSV",
            "13 CSV"
          ]
        },
        {
          "id": "addresses-the-breakdown-gap",
          "why": "--export csv requires --detailed because a quick estimate has no breakdown to export; a plan that never says what a quick export would contain has skipped the actual design problem",
          "type": "regex_any",
          "values": [
            "breakdown",
            "subset",
            "columns",
            "schema"
          ]
        }
      ],
      "deferred": [
        "Whether the reuse is load-bearing in the phases or merely name-dropped — needs a validated judge."
      ],
      "grounding": [
        "skills/csv-export-templates/SKILL.md",
        "commands/estimate.md"
      ]
    },
    {
      "id": 3,
      "prompt": "plan hardening the benchmark harness deny-list — headless bench-live runs use bypassPermissions and the deny list only covers six entries.",
      "assertions": [
        {
          "id": "flags-security-sensitive",
          "why": "The plan must characterise the risk, not merely echo the deny-list noun from the request",
          "type": "regex_any",
          "values": [
            "security-sensitive",
            "secret",
            "exfiltrat",
            "credential"
          ]
        },
        {
          "id": "routes-to-secure-tier",
          "why": "Security-sensitive work routes to --secure regardless of size (skills/request-plan/references/handoff.md)",
          "type": "regex_all",
          "values": [
            "/worktask\\s+--secure"
          ]
        }
      ],
      "deferred": [
        "Whether the proposed additions actually close the bypassPermissions surface — needs a validated judge."
      ],
      "grounding": [
        "benchmark/live/settings/benchmark-settings.json",
        "benchmark/harness/bin/bench-live"
      ]
    }
  ]
}

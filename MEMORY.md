# corpflow Plugin Memory

Repository-tracked memory note (lean rolling format). The authoritative cross-conversation memory lives at `~/.claude/projects/<slug>/memory/MEMORY.md`; full per-release narratives live in git history (`git log --grep="<version>"`) and in the CC band files indexed below. Release tooling reads the `Plugin version:` line — keep its exact format. Hard cap ~5KB: when Release History exceeds 12 rows, delete the oldest.

## Version Tracking

- Plugin version: **4.0.30** — unreleased on `develop` (origin/master is still at 4.0.28), now folding three bodies of work under one version: the 4.0.29 body (wrong PR base discovery and remediation), the thirteen-finding remediation from the leaderboard V1 self-hosting, and the prompt-audit remediation (26 findings across 16 agents, 28 commands, 23 skills; two parallel DV streams addressing tool-grant coverage, normative corrections, and agent-template backfill). See the Release History for full details on all three. The 4.0.29 narrative, retained: a wrong PR base is now blocking rather than invisible. The durable lesson is about what a check is entitled to conclude: `continuity` proved a branch had not diverged from *its recorded* base and was read for years as proving the base was right, which are different claims, and the gap between them let a 27-file run open a 1008-file PR with every check green. The new `base-sanity` compares the would-be PR diff against the run's own ledger — a magnitude cross-check between two independent records of the same run, which is the only thing that could have caught it. New evidence in a shared resolver defaults to opt-in — fork-point fill was first wired as rank 0 for every reader and broke callers that had a correct base already; scoped to the one check built to reconcile it (`--with-fork-point`), it is additive. Second: thirteen remediation items from findings on that same run, unified by one theme: silent failure, writes that land nothing being indistinguishable at the call site from writes that landed, and checks that pass vacuously. `state-patch.sh` now rejects `--facts` per item (the valid remainder persists, exit 2 only when something was rejected) instead of whole-payload; the eviction spill covers resolved items, warns loudly on failure, and triggers on a length comparison rather than a hard-coded `12`; `metadata.description` capped at 240 on the two ledger write paths only, since dispatch-site appends are transient. `handoff-harness.sh` gained a reverse sweep-parity arm (a ledger id absent from the artifact frontmatter was previously invisible), a `files_touched` structural cap (`FILES_TOUCHED_MAX = 10`), and a decision-id divergence check. `fn-preflight` gained a `staging` check for unstaged-on-staged content; the mid-run defect it exposed — the plan's bare ` M` predicate would have fired on every normal FN run — was caught before shipping and replaced with the AR0-specified index/worktree intersection. `hooks/test-execution-gate.sh` gained a `scoped_test_run` arm on the Skill branch; `cli-fallback.sh`'s `.txt` placeholder floor was replaced by loud failure; `attach-visual-evidence.sh` gained a distinct `embed_cap` reason. Four audit-log readers made tolerant and loud instead of silently aborting on the first malformed line. One finding was refuted outright: the screenshot capture scripts already build audit rows with `jq -nc --argjson`, no edit made. A defect was found and fixed mid-run in the fix itself: round 1 shipped a fail-closed false positive where an all-empty `--facts` payload exited 2 as "no valid items", aborting the stage patch for exactly the payload the sweep contract tells stages to emit. Two more issues surfaced by dogfooding `base-sanity` inside this same run, left as follow-ups: it resolved to a stale local `develop` ref nine commits behind `origin/develop` and blocked on a phantom 55-file diff that was actually 0 against the remote-tracking branch, and `facts.files_modified` under-recorded 15 of 33 dirty files, leaving the check's other input unreliable even though it didn't trip the threshold this time. Known follow-up, reproduced four times during this run's own ledger writes: gh#316, the `facts.decisions[]` clamp still evicts silently with no spill file. Carried forward from 4.0.28 — the 26-fix code-review band plus the OV-183 closing-sweep fix: a sweep item's artifact stub and its ledger stub could disagree with nothing looking, because `_sweep_join` ORed `blocks_next_stage` (making `true` unclearable) and `check_sweep_ledger` compared ids only. The durable lesson is a distinction, not a patch — **the raise-only lattice is over LABELLERS, never TRANSPORTS**: `max(agent, orchestrator)` settles two parties labelling one item, while an agent's artifact stub and its own ledger stub are one author with two copies, so a disagreement there is a defect to refuse rather than a lattice to join. Joining them manufactured a boundary gate out of a bookkeeping slip. Carried forward from 4.0.27 — CC 2.1.234→2.1.251, themed on cross-agent and cross-session communication: the resume loop branches on a `SendMessage` delivery result instead of assuming it landed (why the min-CC floor moved), a stage needing a peer session's answer returns `blocked` with `cross_session_ask` for the orchestrator to send (a subagent's reply lands in the parent conversation, never on the stage), a `maxTurns` partial return routes to the existing reattach arm instead of a re-delegate, and a new `PreModelSwitch`/`PostModelSwitch` hook pair gates and records a mid-worktask re-tier away from a stage's pinned `metadata.model` body of work (still unreleased at the time this run started) together with this run's thirteen-finding remediation under one version rather than earning two. See the Release History row below for this run's content. Prior 4.0.29 narrative, retained: a wrong PR base is now blocking rather than invisible. The durable lesson is about what a check is entitled to conclude: `continuity` proved a branch had not diverged from *its recorded* base and was read for years as proving the base was right, which are different claims, and the gap between them let a 27-file run open a 1008-file PR with every check green. The new `base-sanity` compares the would-be PR diff against the run's own ledger — a magnitude cross-check between two independent records of the same run, which is the only thing that could have caught it. New evidence in a shared resolver defaults to opt-in — fork-point fill was first wired as rank 0 for every reader and broke callers that had a correct base already; scoped to the one check built to reconcile it (`--with-fork-point`), it is additive. Second: thirteen remediation items from findings on that same run, unified by one theme: silent failure, writes that land nothing being indistinguishable at the call site from writes that landed, and checks that pass vacuously. `state-patch.sh` now rejects `--facts` per item (the valid remainder persists, exit 2 only when something was rejected) instead of whole-payload; the eviction spill covers resolved items, warns loudly on failure, and triggers on a length comparison rather than a hard-coded `12`; `metadata.description` capped at 240 on the two ledger write paths only, since dispatch-site appends are transient. `handoff-harness.sh` gained a reverse sweep-parity arm (a ledger id absent from the artifact frontmatter was previously invisible), a `files_touched` structural cap (`FILES_TOUCHED_MAX = 10`), and a decision-id divergence check. `fn-preflight` gained a `staging` check for unstaged-on-staged content; the mid-run defect it exposed — the plan's bare ` M` predicate would have fired on every normal FN run — was caught before shipping and replaced with the AR0-specified index/worktree intersection. `hooks/test-execution-gate.sh` gained a `scoped_test_run` arm on the Skill branch; `cli-fallback.sh`'s `.txt` placeholder floor was replaced by loud failure; `attach-visual-evidence.sh` gained a distinct `embed_cap` reason. Four audit-log readers made tolerant and loud instead of silently aborting on the first malformed line. One finding was refuted outright: the screenshot capture scripts already build audit rows with `jq -nc --argjson`, no edit made. A defect was found and fixed mid-run in the fix itself: round 1 shipped a fail-closed false positive where an all-empty `--facts` payload exited 2 as "no valid items", aborting the stage patch for exactly the payload the sweep contract tells stages to emit. Two more issues surfaced by dogfooding `base-sanity` inside this same run, left as follow-ups: it resolved to a stale local `develop` ref nine commits behind `origin/develop` and blocked on a phantom 55-file diff that was actually 0 against the remote-tracking branch, and `facts.files_modified` under-recorded 15 of 33 dirty files, leaving the check's other input unreliable even though it didn't trip the threshold this time. Known follow-up, reproduced four times during this run's own ledger writes: gh#316, the `facts.decisions[]` clamp still evicts silently with no spill file. Carried forward from 4.0.28 — the 26-fix code-review band plus the OV-183 closing-sweep fix: a sweep item's artifact stub and its ledger stub could disagree with nothing looking, because `_sweep_join` ORed `blocks_next_stage` (making `true` unclearable) and `check_sweep_ledger` compared ids only. The durable lesson is a distinction, not a patch — **the raise-only lattice is over LABELLERS, never TRANSPORTS**: `max(agent, orchestrator)` settles two parties labelling one item, while an agent's artifact stub and its own ledger stub are one author with two copies, so a disagreement there is a defect to refuse rather than a lattice to join. Joining them manufactured a boundary gate out of a bookkeeping slip. Carried forward from 4.0.27 — CC 2.1.234→2.1.251, themed on cross-agent and cross-session communication: the resume loop branches on a `SendMessage` delivery result instead of assuming it landed (why the min-CC floor moved), a stage needing a peer session's answer returns `blocked` with `cross_session_ask` for the orchestrator to send (a subagent's reply lands in the parent conversation, never on the stage), a `maxTurns` partial return routes to the existing reattach arm instead of a re-delegate, and a new `PreModelSwitch`/`PostModelSwitch` hook pair gates and records a mid-worktask re-tier away from a stage's pinned `metadata.model`
- Claude Code min required: **2.1.251** (README.md is authoritative; pinned to the band top, and this time load-bearing rather than conventional — the resume loop branches on the `SendMessage` non-delivery reporting that lands across 2.1.234–2.1.238)
- Claude Code latest integrated band: **2.1.234→2.1.251**

## CC Feature Band Index

Canonical band files live in the authoritative memory directory (`~/.claude/projects/<slug>/memory/`).

| Band | Canonical file | Plugin release |
|------|----------------|----------------|
| 2.1.234→2.1.251 | cc-features-2.1.234-251.md | v4.0.27 (cross-agent/cross-session comms; delivery-checked reattach; PreModelSwitch gate; min CC → 2.1.251); still current at v4.0.28, which ships no CC-band change |
| 2.1.221→2.1.233 | cc-features-2.1.221-233.md | v4.0.15 (Todo-tool removal → state-ledger cutover; 200-spawn cap gone; fork-by-default) |
| 2.1.216→2.1.220 | cc-features-2.1.216-220.md | v3.37.0 (nesting depth 5→3 + 20-concurrent cap; Opus 5 default; min CC → 2.1.220) |
| 2.1.210→2.1.215 | cc-features-2.1.210-215.md | v3.35.0 (MCP auto-background + spawn cap + "Needs input"; min CC → 2.1.215, version-gate cleanup) |
| 2.1.203→2.1.209 | cc-features-2.1.203-209.md | v3.34.0 (background-agent/worktree stabilization) |
| 2.1.185→2.1.202 | cc-features-2.1.185-202.md | v3.30.0 (background-default dispatch + Sonnet 5) |
| 2.1.176→2.1.183 | cc-features-2.1.176-183.md | v3.24.0 (agent-teams API) |
| 2.1.171→2.1.175 | cc-features-2.1.171-175.md | v3.17.0 (nested sub-agents) |
| 2.1.166→2.1.170 | cc-features-2.1.166-170.md | v3.13.0 (Fable 5) |
| 2.1.157→2.1.165 | cc-features-2.1.157-165.md | v3.12.0 |
| 2.1.151→2.1.156 | cc-features-2.1.151-156.md | v3.10.13 (top-Opus refresh) |
| 2.1.143→2.1.150 | cc-features-2.1.143-150.md | v3.10.6 |
| 2.1.141→2.1.142 | cc-features-2.1.141-142.md | v3.9.3 |
| 2.1.129→2.1.140 | cc-features-2.1.129-140.md | v3.9.2 |
| 2.1.122→2.1.128 | cc-features-2.1.122-128.md | v3.8.1 |
| 2.1.115→2.1.121 | cc-features-2.1.115-121.md | v3.6.1 |
| 2.1.102→2.1.114 | cc-features-2.1.102-114.md | v3.6.0 |
| 2.1.92→2.1.101 | cc-features-2.1.92-101.md | v3.5.0 |
| 2.1.87→2.1.91 | cc-features-2.1.87-91.md | v3.4.0 |
| 2.1.77→2.1.86 | cc-features-2.1.77-86.md | v3.3.0 |
| 2.1.51→2.1.76 | cc-features-2.1.51-76.md | v3.1.0/v3.2.0 |

> Band files before 2.1.221 are missing from disk — plausibly collateral from the CC 2.1.228 bug "session cleanup deleting contents inside a project's memory folder". Index rows kept as the record of what existed.

## Release History (last 12, newest first)

- 2026-09-08: v4.0.30 (cont.) — **Also carries prompt-audit remediation:** closes 26 findings (8 Critical, 18 Warnings) across 16 agents, 28 commands, 23 skills (score 65/100). Two development streams: infrastructure tool-grant coverage (5 worktask + 3 megatask scripts, audit command grants, G3 gate polarity, model: opus, WCAG 2.2, related: file-relative links, two new predicates `skill-refs.bats` with 14 tests) and agent template backfill (Example Interactions on 15 agents, Rationalizations + Red Flags on 10, command gaps on 5). Full suite for combined release: 1872 bats + 379 Python, exit 0.
- 2026-09-08: v4.0.30 — thirteen findings from the leaderboard V1 self-hosting run, four parallel
  development streams (dedupe/audit-hook keying, ledger-writer clamps, boundary harness,
  contracts/commands/schema), plus gh#316. **Breaking, no migration, decided in-run
  (sw-PL0-3, option A — new runs only):** `handoffs` re-keyed `<PREV_CODE>→<TASK_ID>` (a
  fan-out now writes one edge per task instead of collapsing to one); `facts.decisions`/
  `facts.open_questions` clamps repartitioned per task id (8/4), retiring the global rings of
  8/12. First authority-clean full-suite run of the sequence: 1855 bats green, exit 0, 423
  `@test` cases added over `develop`. Beyond-brief, disclosed rather than fixed: `run-tests.sh`
  has five widening paths to the full suite and only the weakest is guarded — deferred to its
  own worktask by user decision; the cost is that no DV full-suite evidence in any run is
  authority-clean until it lands. `scan-secrets.sh` exits 133 (SIGTRAP) on trees above ~150
  files, outside its `{0,1,2}` contract and bypassing its own fail-closed arm — a cumulative
  interaction, not the anticipated BSD-grep trap; no automated secrets Pass was obtained this
  run, compensated by SR's manual line-by-line pass. Self-hosting confirmed the retired global
  decisions ring was harmful: PL0's and TL0's decisions (`pd1`-`pd5`, `tl1`-`tl3`) were evicted
  pre-fix with no spill to recover them — readable from `planning-0.md`/`coordination-0.md`
  only, not the ledger.
- 2026-09-03/04: v4.0.29 (unreleased, still on `develop` only) — wrong PR base on stacked branches (#351) plus 4.0.28 run remediation (13 findings, 1 refuted), landed together under one version since master had not yet released 4.0.29. Base fix: a PR opened against a branch the work never forked from carried 156 commits and 1008 files into `develop` while the run's own ledger recorded 27, and every finalization check passed because none compared the two. New blocking `fn-preflight base-sanity` cross-checks the would-be PR diff against `facts.files_modified` (fail when `pr_files > ledger*3` **and** `pr_files - ledger > 20`), degrades on seven rungs, runs last in `all` so it never suppresses `continuity`'s row, and has an audited `FN_BASE_SANITY_OVERRIDE`. New `fork_base()`/`base_ref_source()` in `branch-lib.sh`; fork-point fill is **opt-in** (`--with-fork-point`, `base-sanity` the sole caller) after the every-reader version regressed `refine-branch-target`. Disagreement between configured base and fork point raises a PL0 sweep item rather than retargeting silently. Docs: host-declared target documented as an explicit rank across three files, `continuity`'s description corrected to say it does not discriminate a wrong base. Remediation: `state-patch.sh` per-item `--facts` rejection (valid remainder persists, exit 2 only on a real reject), eviction spill now covers resolved items and warns loudly, `metadata.description` capped at 240 on the two ledger write paths; `handoff-harness.sh` gained reverse sweep-parity, a `files_touched` structural cap (`FILES_TOUCHED_MAX = 10`), and a decision-id divergence check; `fn-preflight` gained a `staging` check (shipped as AR0 amended it — the plan's bare ` M` predicate would have fired on every normal FN run, replaced with the index/worktree-intersection form); `test-execution-gate.sh` gained a `scoped_test_run` Skill-branch arm; `cli-fallback.sh`'s `.txt` placeholder floor replaced by loud failure; `attach-visual-evidence.sh` gained a distinct `embed_cap` reason; four audit-log readers made tolerant and loud instead of silently aborting on the first malformed line. Refuted: screenshot capture scripts already use `jq -nc --argjson`, no edit made. Mid-run self-defect caught and fixed before shipping: round 1's all-empty `--facts` payload wrongly exited 2 as "no valid items". New from dogfooding `base-sanity` on this same run: it resolved a stale local `develop` ref nine commits behind `origin/develop`, blocking on a phantom 55-file diff (0 files against the remote-tracking branch); `facts.files_modified` under-recorded 15 of 33 dirty files. Follow-up: gh#316, `facts.decisions[]` clamp eviction still has no spill file, reproduced four times by this run's own writes. Three private base-ref ladders (`dv-tree-preflight.sh`, `attachments-preseed.sh`, `adhoc-visual-evidence.sh` — the last still falling back through hardcoded `origin/main`/`origin/master`/`main`/`master` literals) left divergent as documented consolidation debt.
- 2026-09-03: v4.0.28 — closing-sweep transport divergence (OV-183): `blocks_next_stage` was sticky-`true` and unclearable because `_sweep_join` could not tell an absent key from an explicit `false`, and `check_sweep_ledger` never compared anything but `id`, so the artifact and the ledger disagreed silently and the orchestrator gated on the ledger's copy. Fixed on three surfaces — stickiness now keys on `has`, the harness compares `class` and `blocks_next_stage` and refuses (never reconciles), and the raise-only lattice is scoped to labellers with transports named a defect. `blocks_next_stage` became a required stub field on both transports, which is what removes the absent-vs-false ambiguity at the source. Also carries the 26 code-review fixes previously staged as Unreleased.
- 2026-08-29: v4.0.27 — CC 2.1.234→2.1.251 cross-agent/cross-session band; delivery-checked reattach, cross_session_ask handoff, PreModelSwitch gate; min CC → 2.1.251.
- 2026-08-26: v4.0.26 — command-surface reorganization, no aliases: 38 → 28 commands, 26 → 23 registered skills. Six report/PM commands plus `/worktask-status` removed — `status-view.sh` and its bats go in the same commit, since coverage-proxy C2 reddens on a script with no `.bats` and C5 on an orphaned one. `/context-status` removed too: it could not perform any part of its own contract, because a model cannot observe its own token accounting and `--compress` held no `Write` grant to apply anything with. Cost observability retired entirely after confirming no shipped hook ever wrote the `cost-*.jsonl` `/cost-report` aggregates, so it was inert from release; `pm-` prefix dropped from milestone/roadmap/sprint/product-requirements; `dev-code-review` → `tech-code-review` (its old name existed only to disambiguate CC's built-in `/code-review`); `senior-developer-review` folded into `estimation-methodology/references/estimate-review.md` and wired as an inline `--detailed` step; App Store publishing moved to apple-developer 1.30.0, Play Store publishing authored in android-developer 1.5.0, both behind a new `/appstore` dispatching `release-engineer` (now sonnet/maxTurns 40 with a bare `Task` grant). Fixed first: `worktask/SKILL.md` ordered `Skill("dev-code-review")` against a skill that never existed while `skill-refs.bats` globbed only `agents/` + `commands/` and could not see it; no test read a path-scoped `Bash(<path>)` grant against disk; `cross-plugin-refs.bats` resolved siblings at `$PLUGIN_ROOT/..`, found none, and passed while verifying nothing (now `CORPFLOW_SIBLING_ROOT` + a CI clone step).
- 2026-08-24: v4.0.25 — the plan-approval carrier `PL0.metadata.approved` had two readers and no writer, so a real human approval at the plan gate left the first DV turn blocked and a prior run had to hand-stamp the field mid-pipeline. `commands/worktask.md` now stamps at all three arms where approval is established (approval `user`, pure-bypass `auto`, escalate-resolution `user`), each ordered before its audit row because the reverse crash window resumes into the stage loop with the carrier unset; Step A.4 is documented as deliberately non-stamping; `commands/megatask.md`'s per-issue seed stamps `auto`; the two `agents/developer.md` readers and the one in `agents/product-manager.md` are carrier-qualified (a bare reference reads as the reading task's own metadata and would leave the fix inert); the ledger schema entry gained a `description`. New `tests/shell/worktask/approval-gate.bats` extracts writer payload, reader set and schema enum at test time with three anti-vacuity guards.
- 2026-08-22: v4.0.23 — CI pipeline established from scratch (repo had no CI at all): `.github/workflows/test.yml` added with shell-bash pipefail gate so red test suites report red to GitHub (critical DR-P0 fix), new `tests/shell/meta/ci-workflow.bats` (16 assertions including fetch-depth per-job regression test, independently mutation-verified non-vacuous), matrix-row R28 for CI workflow tests, four stale "not wired to CI" claims cleared across three docs, disabled model invocation on two pipeline-only skills (`preview-ensurer`, `csv-export-templates`), manifest keywords reduced 120→15 moving release-note tokens to CHANGELOG.md, marketplace.json mirrored keywords + version from plugin definition, gate labels disambiguated. The only runtime-behaviour fix in the release: `state-patch.sh`'s ENOSPC guard probed with BSD-only `df -Pg`, which GNU df rejects, so the `DISK_MIN_GB` hard halt had never been reachable on Linux; now POSIX `df -Pk`.
- 2026-08-21: v4.0.22 — superpowers-patterns adoption: 42 `description:` frontmatter fields (16 agents + 26 skills) rewritten to a trigger-first grammar (commands exempt, length cap only); rationalization tables + Red Flags added to 6 agents (developer, product-manager, project-manager, qa-engineer, technical-lead, workflow-engineer), strictly additive; `desc-lint.sh` grew 1 check → 6 (G1–G6), asset discovery switched to `find skills -name SKILL.md`, `desc-lint.bats` 10 → 26 cases; authoring doctrine consolidated in `agents/prompt-engineer.md`; PL stage made proactive — announced sizing, assumption-tagged plan-shaping questions, one-way `requests_stage_escalation` re-sizing ratchet, stage-task right-sizing. Collateral: one skill's YAML-invalid `description:` frontmatter now parses.
- 2026-08-21: v4.0.21 — routing matrix + project override: `skills/shared/routing-matrix.md` is the single copy of alias→`plugin:agent` targets (inline stage-agent tables demoted to bats-validated mandated copies via `routing-matrix.bats`); a user-project-root `CORPFLOW.md § Routing` overrides per alias, persisted as `state.routing` at worktask init; bare `Task` grants replace ~60 literal grants in developer/software-architector/security-reviewer/qa-engineer; `## Routing` heading reserved (plugin-side template guard). Collateral fixes: stale android bare names + missing apple DR row in `plugin-protocols.md`, pm-milestone apple default, dead `workflow-integration` column.
- 2026-08-21: v4.0.20 — prompt-surface compression, 145 files 240k→193k words (#313): commands −27.6%, agents −20.9%, skills −16.3%; dedup one-directional into canonical copies with 605 citations re-verified; collateral fixes include the drifted `stage-codes.md § Model Lookup` table and the stale `stage-contracts.md` atomic-merge snippet.
- 2026-08-17: v4.0.19 — the three plugin rules the OV-184 test-strategy violation exposed: brief invocations derive from `<plan_file>` frontmatter (never a project README's core-commands snippet, which is full-suite-shaped and pre-empts QA's gate), `build-only` is selected rather than opt-in when the planned diff is comment/doc-only (and a post-QA doc-only change never re-triggers QA), and FN reverts build-tool churn as its last action before `git add`. The mechanical gate needed no change — it already denied the incident's exact `xcodebuild test -destination '…'` shape.

## Token Baselines

Authoritative per-surface baselines: `skills/cost-optimization/references/token-baselines.md`. This file no longer mirrors them.

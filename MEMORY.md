# igrsoft Plugin Memory

Repository-tracked memory note. The authoritative cross-conversation memory lives at `~/.claude/projects/<slug>/memory/MEMORY.md`; this file mirrors version-tracking + optimization history for in-repo discoverability and CI checks (e.g. release tooling that reads `version:` from this file).

## Version Tracking

- Plugin version: **3.10.0** (Behavioural activation of CC 2.1.110→2.1.142 communication features on branch `feature/dallas-v3`; minor bump — runtime behaviour change, new on-disk artifact `state.checkpoint-*.json`, new operator surface via `mcp_tool` PushNotification. v3.9.1 was the last in-repo-tracked patch; bands v3.9.2 → v3.9.4 were doc-only and tracked in the personal `~/.claude/projects/.../MEMORY.md` between releases.)
- Claude Code latest known: 2.1.142 (2026-05-15)
- Claude Code min required: 2.1.114 (mcp_tool hook in `Stop` block needs 2.1.118 but degrades gracefully — feature-degraded, not broken — keeping the floor lower)
- Location: `/Users/korich/Projects/igrsoft/company-workflow`

## Optimization History

- **2026-05-15**: v3.10.0 — **Behavioural activation of CC 2.1.110→2.1.142 communication features**. Branch: `feature/dallas-v3`. Prior bands v3.6.0→v3.9.4 integrated CC features as *documentation only* (band files + `references/hook-monitoring.md`). Audit of 20 high-value features showed 14 documented but never wired. This release moves the high-leverage **inter-agent communication** primitives from prose to plugin-enforced runtime. **Shipped** (5 items): (A) `.claude-plugin/plugin.json` now ships `hooks` block — `PostToolUse` (matcher `TaskUpdate|TaskCreate|Write|Edit`, `continueOnBlock: true`, `args` exec-form) + `SubagentStop` + `PreCompact` + `Stop` (matcher `igrsoft:product-manager|igrsoft:project-manager`, `type: "mcp_tool"`, server `conductor`, tool `PushNotification`). (B) Four new plugin-shipped hook scripts in `hooks/`: `audit-tooluse.sh` (writes `tool_invoked` rows with `duration_ms` from v2.1.119 + `effort` from v2.1.133), `audit-subagent.sh` (replaces prose-instructed `subagent_stopped` rows), `precompact-checkpoint.sh` (copies `.context/state.json` to `.context/state.checkpoint-<ts>.json` on every compaction, never blocks), `agent-stop.sh` (Stop multiplexer for PL/FN/ST writing `stage_completion_hook` rows). Each ships a `--self-test` flag (4/4 pass). (C) Agent-frontmatter `hooks:` (v2.1.116+) added to PL/FN/ST agents only — `product-manager.md` → `--stage PL`, `project-manager.md` → `--stage FN`, `stakeholder.md` → `--stage ST`. Full per-stage rollout deferred to v3.10.1 (one-line additions, no behavioural delta). (D) `skills/cost-optimization/SKILL.md § Capture Script` extends `cost-log.sh` jq schema with `effort` field sourced from `CLAUDE_EFFORT` (v2.1.133+); `commands/cost-report.md` gains `### Effort Distribution` table (rows per `(stage, effort)`) + Data Source line. (E) `mcp_tool` hook fires conductor `PushNotification` at PL/FN approval gates — gracefully no-ops when server absent. **Backward-compat / dedupe** (documented in `skills/agent-coordination/SKILL.md § Writers`): hook rows carry `actor: "hook:<name>"` + `metadata.dedupe_key`; agent-emitted rows for same action become advisory. Reader rule: when both rows share dedupe_key, hook row wins. Dedupe-key shapes for `tool_invoked` / `subagent_stopped` / `stage_completion_hook` documented. Action enum in audit schema extended with `tool_invoked|precompact_checkpoint|stage_completion_hook`. **Skipped** (with one-line rationale each): `/goal` (duplicates `state.json:facts.goal`), `mcpServers:` agent frontmatter (current `tools:` discipline already enforces MCP scope), `Skill(name *)` wildcard tightening (no over-broadened access today), `terminalSequence` hook field (local UX only), per-agent `hooks:` for 13 remaining stage agents (deferred v3.10.1 — already SubagentStop-covered), `claude agents` dispatch flags (already wired in v3.9.4 metadata bridge), `/recap`/`/feedback` re-wiring (out of audit scope), PostCompact reactive seeding script (existing `state.json` resume protocol covers once PreCompact checkpoints exist). **Files modified**: 12 (`hooks/audit-tooluse.sh` NEW, `hooks/audit-subagent.sh` NEW, `hooks/precompact-checkpoint.sh` NEW, `hooks/agent-stop.sh` NEW, `.claude-plugin/plugin.json`, `agents/product-manager.md`, `agents/project-manager.md`, `agents/stakeholder.md`, `skills/agent-coordination/SKILL.md`, `skills/agent-coordination/references/hook-monitoring.md`, `skills/cost-optimization/SKILL.md`, `commands/cost-report.md`) plus version bump + MEMORY narrative. README min CC unchanged at 2.1.114 (mcp_tool hook needs 2.1.118 but degrades gracefully). Minor bump rationale: runtime behaviour change, new on-disk artifact (`state.checkpoint-*.json`), new operator surface (PushNotification).

- **2026-05-13**: v3.9.1 — **Handoff Protocol centralization + cache observability + drift lint**. Branch: `feature/chengdu-v5`. **Goals**: (1) cut ~245 lines of duplicated Handoff Protocol boilerplate across 13 stage agents (Required Inputs prose, F1 fallback paragraph, Frontmatter Template intro, Completion Verification checklist — single source moves to `skills/shared/stage-contracts.md`); (2) close AC-14 observability gap by capturing `cache_read_input_tokens` + `cache_creation_input_tokens` in `cost-log.sh` hook and surfacing them in `/cost-report § Cache Performance`; (3) prevent future drift via new `cache-lint.sh --frontmatter-template-lint` mode. **Key changes**: `skills/shared/stage-contracts.md` gained `## Per-Stage Frontmatter Templates` (13 anchors `#tpl-pl`..`#tpl-et`), `## Completion Verification (handoff-protocol)`, `#run-index-resolution` and `#f1-telemetry` anchors, plus a "agents MUST NOT restate this" admonition; 13 stage agents (`product-manager`, `software-architector`, `team-lead`, `developer`, `technical-lead`, `security-reviewer`, `qa-engineer`, `technical-writer`, `release-engineer`, `project-manager`, `stakeholder`, `incident-responder`, `ethics-reviewer`) collapsed `## Handoff Protocol` section from ~30–40 lines to ~14–17 lines (boilerplate gone, YAML payload + `Prev→this` label kept inline for paste-source ergonomics; AR/TL retain skip-exploration short-circuit note); `skills/cost-optimization/SKILL.md § Capture Script` extended `cost-log.sh` jq schema with `cache_read_input_tokens` + `cache_creation_input_tokens` (sourced from `CLAUDE_CACHE_READ_INPUT_TOKENS` / `CLAUDE_CACHE_CREATION_INPUT_TOKENS`, available in CC 2.1.114+); `commands/cost-report.md` got `### Cache Performance` table (hit_ratio = cache_read / (input + cache_read), flag rows <60%, count F1 fallbacks from `.context/logs/fallback-*.log`) + Data Source update; `skills/workflow/references/cache-lint.sh` gained `--frontmatter-template-lint` mode asserting (a) `## Handoff Protocol` exists, (b) exactly one fenced yaml block inside, (c) `handoff:` + `stage:` present, (d) stage matches the canonical agent→stage map, (e) block ≤30 lines. Self-test exercises positive + stage-mismatch + duplicate-block cases. **Verification**: `handoff-harness.sh --self-test` and end-to-end run still PASS (98–99% reduction, AC-12 met); `cache-lint.sh --self-test` 7/7 PASS; `cache-lint.sh --frontmatter-template-lint agents/*.md` passes all 13 agents (block sizes 10–14 lines). `grep` confirms zero re-introduction of `state.json absent`, atomic-write pseudocode, `### Required Inputs (handoff-protocol)`, `### Completion Verification (handoff-protocol)`, or `### Frontmatter Template` in `agents/*.md`. Files modified: 18 (`skills/shared/stage-contracts.md`, 13 agents, `skills/cost-optimization/SKILL.md`, `commands/cost-report.md`, `skills/workflow/references/cache-lint.sh`, `.claude-plugin/plugin.json`, `MEMORY.md`). Drift surface for Handoff Protocol: 13 files → 1 file. No runtime contract change — agents read the same `state.json` and write the same `handoff:` frontmatter; only the prose location moved.

- **2026-05-08**: v3.9.0 — **Test Selection Gate + Selective Retest Strategy**. Replaces binary `requires_ui_tests` flag with three-mode `test_mode: build-only|scoped|full` plus inline source markers (`@test-required`, `@depends-on: <Symbol>`, `@test-tag:`) parsed by DV to produce a Selected Tests list consumed by QA. Branch: `feature/seattle-v4`. **Goals**: (1) reduce wall-time when full regression isn't needed (`build-only`/`scoped` defaults); (2) per-test required markers + symbol-level dependency declarations so a change to `Foo` retests `FooTests` AND any test marked `// @depends-on: Foo`. **Key changes**: rewrote `skills/shared/testing-strategy.md § UI Test Gate → § Test Selection Gate` (3-mode model + auto-promotion safety nets); new `skills/shared/test-selection-syntax.md` (marker grammar, parser pseudocode, "covers" rule, Swift Testing trait equivalents, platform-handler matrix); updated `agents/product-manager.md § Required Metadata` (PL writes `test_mode` + `always_required_tests` + `ui_visual_check` per complexity-score heuristic); updated `agents/developer.md` D2 (selection algorithm, smoke-set execution in build-only, positive `-only-testing:` instead of blanket `-skip-testing:`, auto-promotion when no platform handler) + D1.5 (annotate new tests); updated `agents/qa-engineer.md` Q1 (3-mode dispatcher reads DV's `§ Selected Tests`, auto-promotes empty selection to scoped, ingests warnings); added DR responsibility in `agents/technical-lead.md` to surface non-empty `.context/logs/test-selection-warnings.md` as findings; updated `commands/{test-plan,test-coverage,qa-report}.md` (annotation columns, marker-coverage report, selection breakdown). **Reviewer-driven adjustments** (parallel `agent-teams:team-reviewer` correctness + DX/cost dimensions): (a) effective default for unannotated plans is `scoped`, not `build-only` — opt-in for build-only avoids silent coverage blackout on untagged repos; (b) `requires_ui_tests: false` legacy alias maps to `scoped` (not `build-only`) to preserve today's "DV scoped, QA full unit+integration" semantics; (c) `build-only` mode at DV runs the smoke set (`@test-required` + `always_required_tests`) — not zero tests — to preserve fast feedback for critical paths; (d) auto-promotion safety nets cover empty-selection AND missing-platform-handler scenarios; (e) "covers changed files" rule made explicit in `test-selection-syntax.md`; (f) DR reads warning log to catch silent test drops; (g) cost target re-stated as wall-time reduction (measured at verification) rather than the unsupported "≥30% token drop" claim. Files modified: 8 (`skills/shared/testing-strategy.md`, `skills/shared/test-selection-syntax.md` NEW, `agents/{product-manager,developer,qa-engineer,technical-lead}.md`, `commands/{test-plan,test-coverage,qa-report}.md`) + version bump (`.claude-plugin/plugin.json`, `MEMORY.md`). One-release backward-compat alias for `requires_ui_tests`. No agent role changes; QA owns auto-promotion when DV's selection is empty.

- **2026-05-08**: v3.8.2 — Workflow orchestration token + communication enhancement. Branch: `feature/hong-kong`. **A3** dedupe model cost tier table: removed duplicate from `skills/cost-optimization/SKILL.md` (links to `skills/shared/model-selection.md` § Cost Tiers as single source of truth). **A4** added per-effort thinking-budget ceilings matrix (`low ≤4K`, `medium ≤16K`, `high ≤32K`, `xhigh ≤50K`, `max ≤64K`) to cost-optimization SKILL.md — justifies existing `effort: xhigh` on prompt-engineer/software-architector/security-reviewer. **B1** anchor pre-flight: documented PostToolUse hook on `Write|Edit` of `.context/*-N.md` artifacts (catches missing anchors at producing stage instead of DR-gate post-hoc) in `skills/workflow/references/handoff-protocol.md § Anchor Pre-Flight`. **B2** `metadata.skip_exploration` + `metadata.exploration_anchors` propagation: PL0 stamps both on every downstream task when `.context/exploration.md` exists; AR/TL agents short-circuit redundant Glob/Grep. Documented in `agents/product-manager.md § Downstream propagation`, `skills/agent-coordination/SKILL.md § metadata.skip_exploration Propagation`, and consumed by `agents/software-architector.md` + `agents/team-lead.md` Handoff Protocol § Required Inputs. **B3** F1 fallback telemetry: agents emit one line to `.context/logs/fallback-${run_index}.log` when state.json is absent — surfaces silent cache degradation; documented in `skills/shared/stage-contracts.md`. **B4** error-file lazy-create guard: orchestrator validation step 7 now treats missing `metadata.error_file` on disk as `retry_count = 0` (not failure); first-appending agent creates the file. **B5** mandatory plan-file anchor schema: `agents/product-manager.md § Mandatory Plan-File Anchor Schema` lists all seven required H2 anchors per `handoff-protocol.md#anchor-allow-list § PL`. **Skipped from original plan**: A1 (constraint preamble) — only developer.md has the elaborate preamble; no extractable duplication. A2 (workflow integration template) — sections are stage-specific content, not boilerplate. A5 (logging dedup) — agents already reference `logging-conventions` skill; no kind/scope tables to consolidate. Files modified: 7 (`skills/cost-optimization/SKILL.md`, `agents/product-manager.md`, `agents/software-architector.md`, `agents/team-lead.md`, `skills/agent-coordination/SKILL.md`, `skills/shared/stage-contracts.md`, `skills/workflow/references/handoff-protocol.md`) + version bump (`.claude-plugin/plugin.json`, `MEMORY.md`). No agent role changes; no breaking contract changes. Estimated impact: ~1-3K token savings per workflow (B2 short-circuit on AR/TL); fallback cost ↓ ~10× when anchors absent (B1 hook); cache hit rate target 65% (was ~60%).
- **2026-05-03**: v3.8.0 — Stage artifacts now numbered (`analyzing-N.md`, `coordination-N.md`, `development-N.md`, `developer-review-N.md`, `security-review-N.md`, `testing-N.md`, `documentation-N.md`, `release-N.md`, `complete-summary-N.md`, `retrospective-N.md`, `incident-N.md`, `ethics-review-N.md`) inheriting N from `planning-N.md` via `metadata.run_index` (integer stamped by PL0 on every downstream task). PL0 atomically resets `state.json` (`run_index`, `stages`, `facts`) on new run. Subdirs (`errors/`, `logs/`, `designs/`, `images/`) and `state.json` stay shared across runs. Legacy unnumbered names accepted as fallback for one release cycle. Four §4 renames applied: `release-prep.md`→`release.md`, `complete.md`→`complete-summary.md`, `incident-report.md`→`incident.md`, `approval.md`→`retrospective.md`. Branch: `feature/helsinki`.
- **2026-05-01**: v3.7.0 — handoff-protocol redesign: `state.json` ledger + `handoff:` YAML frontmatter schema + cache-friendly preamble layout `[1][2][3][4][5][6][7]` across 12 stage agents + 7 skills + 1 command + 4 new files. Branch: `feature/handoff-protocol`. (24 in-scope edits + 2 version-bump edits = 26 files: 4 NEW + 22 EDIT.)
  - **NEW** `skills/workflow/references/handoff-protocol.md` — canonical spec.
  - **NEW** `skills/workflow/references/cache-lint.sh` — Bash POSIX prefix-stability + anchor-lint with `--self-test` (passes).
  - **NEW** `skills/workflow/references/handoff-harness.sh` — Bash POSIX token-count harness (≥30% reduction confirmed on synthetic fixtures: DV 99%, DR 99%, QA 98%, DC 98%, FN 99%) + frontmatter/state.json schema validators + idempotency check.
  - **NEW** `.claude/hooks/state-merge.sh` — SubagentStop hook (idempotent atomic merge, exits 0 always, yq + awk fallback, `--self-test` passes).
  - **EDIT** `skills/shared/stage-contracts.md` + `skills/shared/task-system.md` — anchor-based Required Inputs, frontmatter mandate in Required Outputs, `metadata.context_refs` + `metadata.state_file` added with legacy `context_files` retained as F1 fallback.
  - **EDIT** `skills/workflow/SKILL.md` — Step 0 (read state.json), Step 6.5 (post-Task atomic merge), banner relocation prefix→suffix, cache-friendly layout documented.
  - **EDIT** `skills/workflow/references/initialization-patterns.md` — PL0 state.json init snippet, sample `context_refs` TaskCreate.
  - **EDIT** `skills/context-compression/SKILL.md` + `skills/cross-plugin-handoff/SKILL.md` + `skills/cost-optimization/SKILL.md` — state-ledger compression primitive, full-schema mandate for cross-plugin agents (with `## relaxed-profile` stub), `ENABLE_PROMPT_CACHING_1H=1` recommendation + state-merge.sh hook documentation.
  - **EDIT** 12 stage agents: `agents/{product-manager,software-architector,team-lead,developer,technical-lead,security-reviewer,qa-engineer,technical-writer,release-engineer,project-manager,stakeholder,incident-responder}.md` — `## Handoff Protocol` section appended (Snippet A verbatim, Frontmatter Template per stage, Snippet B with substituted placeholders). DR Checks 1/2/3/4 grep-pass: 12/12/1/1.
  - **EDIT** `commands/workflow.md` — Phase 1 step 3a inserts atomic state.json seed.
  - **EDIT** `.claude-plugin/plugin.json` — version `3.6.1` → `3.7.0`.
  - Backward-compat: paths F1 (state.json absent), F2 (agent ignores it), F3 (no frontmatter), F4 (corrupt state.json) all documented and exercised.
- **2026-05-01**: UI Test Gate — DV step D2 + QA step Q1 + QA Visual Comparison gated on `metadata.requires_ui_tests=true` in `<plan_file>`. Branch: feature/uitest-gate.
- 2026-05-03: FN-gate pre-seed fix — hoisted Pre-gate Conductor-attachments writer from pseudocode loop body into first-class SKILL.md H3; restructured conductor-attachments.md § When to write into Writer 1/Writer 2 subsections. Branch: feature/fn-gate-preseed-fix. No version bump (workflow doc restructuring).
- 2026-04-29: PL produces numbered `planning-N.md`.
- 2026-04-28: v3.6.1 — 2.1.115-121 integration.
- 2026-04-20: ST self-improvement skill + `/improve-yourself` manual command.
- 2026-04-20: `.context/logs/` canonical subfolder + `logging-conventions` skill.
- 2026-04-20: v3.6.0 — 2.1.102-114 integration.
- 2026-04-12: v3.5.0 — 2.1.92-101 integration.
- 2026-04-03: v3.4.0 — 2.1.87-91 integration.
- 2026-03-28: v3.3.0 — 2.1.77-86 integration.
- 2026-03-15: v3.2.0 — 2.1.72-76 integration.
- 2026-03-08: v3.1.0 — 2.1.51-71 integration.
- 2026-03-03: v3.0.0 — multi-agent-optimize patterns.
- 2026-02-06: Agent prompts optimization (constraint-first).
- 2026-01-31: Token optimization via shared references.

# igrsoft Plugin Memory

Repository-tracked memory note (lean rolling format). The authoritative cross-conversation memory lives at `~/.claude/projects/<slug>/memory/MEMORY.md`; full per-release narratives live in git history (`git log --grep="<version>"`) and in the CC band files indexed below. Release tooling reads the `Plugin version:` line — keep its exact format. Hard cap ~5KB: when Release History exceeds 12 rows, delete the oldest.

## Version Tracking

- Plugin version: **3.33.0** (new skill `gh-issue-dedup` + `publish-pl-issue.sh` cross-run dedup: one `.context/` ↔ one GitHub issue via the run-independent `.context/gh-issue.json` anchor. A later worktask in the same `.context/` comments on the existing issue instead of opening a duplicate; exact-title single-hit `gh issue list --search` recovers a lost anchor. MINOR bump — skills[] registration + version triple + README. See Release History for the prior v3.32.0 command taxonomy refactor.)
- Claude Code min required: **2.1.200** (README.md is authoritative; gate integrity relies on 2.1.200 AskUserQuestion no-auto-continue + worktree plugin loading; Fable alias resolves only on CC ≥ 2.1.170)
- Claude Code latest integrated band: **2.1.185→2.1.202**

## CC Feature Band Index

Canonical band files live in the authoritative memory directory (`~/.claude/projects/<slug>/memory/`).

| Band | Canonical file | Plugin release |
|------|----------------|----------------|
| 2.1.185→2.1.202 | cc-features-2.1.185-202.md | v3.30.0 (background-default dispatch + Sonnet 5) |
| 2.1.176→2.1.183 | cc-features-2.1.176-183.md | v3.24.0 (agent-teams API) |
| 2.1.171→2.1.175 | cc-features-2.1.171-175.md | v3.17.0 (nested sub-agents) |
| 2.1.166→2.1.170 | cc-features-2.1.166-170.md | v3.13.0 (Fable 5) |
| 2.1.157→2.1.165 | cc-features-2.1.157-165.md | v3.12.0 |
| 2.1.151→2.1.156 | cc-features-2.1.151-156.md | v3.10.13 (Opus 4.8) |
| 2.1.143→2.1.150 | cc-features-2.1.143-150.md | v3.10.6 |
| 2.1.141→2.1.142 | cc-features-2.1.141-142.md | v3.9.3 |
| 2.1.129→2.1.140 | cc-features-2.1.129-140.md | v3.9.2 |
| 2.1.122→2.1.128 | cc-features-2.1.122-128.md | v3.8.1 |
| 2.1.115→2.1.121 | cc-features-2.1.115-121.md | v3.6.1 |
| 2.1.102→2.1.114 | cc-features-2.1.102-114.md | v3.6.0 |
| 2.1.92→2.1.101 | cc-features-2.1.92-101.md | v3.5.0 |
| 2.1.87→2.1.91 | cc-features-2.1.87-91.md | v3.4.0 |
| 2.1.77→2.1.86 | cc-features-2.1.77-86.md | v3.3.0 |
| 2.1.51→2.1.76 | cc-features-2.1.51-76.md | v3.1.0/v3.2.0 |

## Release History (last 12, newest first)

- 2026-07-08: v3.33.0 — cross-run GitHub-issue dedup (one `.context/` ↔ one issue). Root cause: `commands/worktask.md` re-seeds `state.json` (metadata wiped) every fresh `/worktask`, so the only dedup anchor (`metadata.github_issue_url`) was lost on each `run_index` increment → a second worktask in the same `.context/` opened a duplicate issue. Fix: new run-independent `.context/gh-issue.json` anchor + a comment-instead-of-create branch in `skills/worktask/references/publish-pl-issue.sh` (`resolve_context_issue_local`/`_search`, `write_context_issue`, `bump_anchor_commented`; guard order opt-out → cross-run resolve → milestone skip → gh/auth/remote). A later run resolves the anchor (or an exact-title **single-hit** `gh issue list --search`, `GH_ISSUE_SEARCH=0` disables; ambiguous multi-hit refused) and posts a marker-deduped follow-up comment (`mode:"comment"`). New skill `skills/gh-issue-dedup` (canonical protocol) + 6 bats (`tests/shell/worktask/gh-issue-dedup.bats`, 13/13 with existing). FN PR-issue-link validator gains the anchor as rank-2. Wired into worktask/SKILL.md, commands/worktask.md, product-manager.md, project-manager.md, task-system.md, task-folder-organization. MINOR: skills[] registration + version triple (plugin.json + marketplace.json ×2) + README (24 skills). ~13 files + 2 new.
- 2026-07-07: v3.32.0 — commands/ directory taxonomy refactor (issue #201, breaking): 46→36 commands unified under one `<domain>-<action>` prefix scheme. 10 files renamed (git mv + frontmatter), 7 near-duplicate commands folded into 5 flag-absorbing survivors (new `--type`/`--depth`/`--lens`/`--review`/`--export` modes), 3 never-used commands deleted outright. No deprecation-alias stubs shipped (breaking change; the 20-token retired→replacement map ships in the DC-stage release notes, not here — cache is version-keyed so plugin.json + marketplace.json (commands array) + README all bump together). 49 files touched across commands/, agents/, skills/, README.md, both manifests.
- 2026-07-07: v3.31.0 — state.json v1 additive upgrade (issue #199): dispatch tracking (`facts.dispatched_agents[]`), `stages.<CODE>.last_error`, `completed_via` provenance, `stages.<CODE>.worktree` record, `model_requested`/`model_resolved`/`model_name` keys, `facts.capabilities` cache, and an mkdir-spinlock in `state-patch.sh` (`--via` provenance) guarding legal sibling-stage overlap on the lockless merge. 9 new bats tests (21/21 total), 5/5 concurrency smoke. 10 files.
- 2026-07-07: v3.30.0 — Claude Code 2.1.185→2.1.202 update (16 files: background-default completion gating, error propagation, gate no-auto-continue, exact-match Stop matcher, Sonnet 5; min CC → 2.1.200).
- 2026-07-06: v3.29.0 — Swift benchmark/tests/TTT migration (issue #196, PR #197). Python suites (37 files, 5,155 LOC: benchmark/lib, benchmark/live, benchmark/tests, tests/python) retired and replaced by three SwiftPM packages: benchmark/ttt-template SwiftUI TicTacToeKit fixture (1,097 src + 665 test LOC, 48 tests macOS/iOS), benchmark/harness BenchmarkKit+BenchmarkLive deterministic+live (2,672 src + 2,056 test LOC, 140 tests), tests/swift PluginScriptsTests (463 LOC, 38 subprocess behaviors). Deterministic mode fully offline (AC-8 link-level confirmed: bench-deterministic cannot link BenchmarkLive module), zero LLM calls in any test (injected seams). Live mode frozen-argv `claude -p` dispatch with dual-mode stream-json/json coverage capture, per-stage token attribution, budget gates (rc 0/2/3/4/64), partial records on rc=4. Metrics byte-parity vs pre-migration via custom ordered JSON serializer (Codable cannot hold key order/explicit nulls/float artifacts). Four commits 53e90a3 (T1) / a2a4595 (T2+T3) / 4a60fb6 (T4) / 3b46a71 (T5) + fix f563b5c (subprocess stdin/stderr). Total 226 Swift tests ≥171 parity target. DV gates G1-G4 green (ttt-template 48 tests iOS pass, harness 140 tests, seams green, prompts green). README + MEMORY + benchmark/README.md + tests/{README,COVERAGE}.md + benchmark/live/prompts/README.md (doc-side only, ~11 files touched). P1 known issue: temp dir leak ($TMPDIR ttt_test_*/bats-run-*) documented in benchmark/README.md pending fix.
- 2026-06-30: v3.28.1 — cache-rebuild bump fixing 3 "skills path not found" load errors (estimation-methodology, pencil-design-worktask, senior-developer-review). Commit bdb9325 renamed those skill dirs (estimation→estimation-methodology, pencil-design→pencil-design-worktask, review→senior-developer-review) + root skills/SKILL.md→README.md and updated all manifest/agent/command/skill cross-refs, but kept version 3.28.0; the plugin cache is version-keyed (`~/.claude/plugins/cache/igrsoft/igrsoft/<ver>/`), so the stale 3.28.0 cache kept the old dirs while the refreshed marketplace manifest pointed at the new paths → the loader resolved new-name paths against the old-name cache and the 3 skills silently failed to register. No structural change — repo skill tree already correct. Patch-bump plugin.json + marketplace.json (metadata + plugins[0]) + README 3.28.0→3.28.1; MEMORY. Manifests/docs only, 4 files.
- 2026-06-23: v3.28.0 — packaging sync: registered dv-screenshot-capture + preview-ensurer skills + PR-creation (command removed in v3.32.0) + improve-yourself commands in marketplace.json (were omitted under strict:true ⇒ DV visual-evidence gate didn't install); plugin.json + marketplace.json 3.27.1→3.28.0; README 3.27.0→3.28.0; de-staled request-plan/evals/evals.json trigger prefixes (quick:/worktask: → /worktask); clarified skills/SKILL.md shared/milestone-helpers load note. Manifest/docs only, 6 files.
- 2026-06-23: v3.27.1 — compact code-documentation standard: new `skills/shared/code-documentation.md` (Write/Do-NOT table, length budgets, where-rationale-belongs map, OverCameraGlass BEFORE/AFTER) defines contract-only source comments and bans doc-comment essays, design-history/Figma/rgba narration, verification logs, and call-site enumerations; wired into DV (`developer.md` 0.6.1→0.6.2: Response Approach #8, a DO-NOT constraint naming `development-N.md § Decisions` as proof, delegation Context Passing), DR (`technical-lead.md` 0.2.2→0.2.3: Code Review Depth comment-density item), DC (`technical-writer.md` 0.1.1→0.1.2: artifact-vs-source reconciliation + completion gate), the DR command (`dev-code-review.md` 0.1.2→0.1.3 (file carried its pre-3.32.0 name at the time; renamed in v3.32.0): P2 maintainability finding + `related:`), and the cross-plugin delegation prompt (`cross-plugin-handoff/SKILL.md` Constraints). plugin.json 3.27.0→3.27.1, marketplace.json 3.26.1→3.27.1. Docs/prompt-only, ~8 files + 1 new.
- 2026-06-22: v3.27.0 — token-optimization consolidation: trimmed 3 tiers (ambient descriptions −555 chars, dead-content removals, prose tightening, F1 centralization); created 4 on-demand shared docs (platform-detection, figma-capture, worktask-stage-context, legacy-fallback-f1; 311 lines one-time cost); externalizations + frontmatter migration (T2-4); removed FAST_MODE_OVERRIDE, old stage codes (TC/PE/DS rows), expired requires_ui_tests table, Tier-D deprecation paths; net −416 lines, 83 files; cache canary 98–99% all stages (AC-12 ≥60%); all 4 guardrails green (desc-lint, cache-lint, handoff-harness, prompt-audit); 0 regressions; 6 safety contracts intact. plugin.json 3.26.1→3.27.0. ~83 files + 4 new.
- 2026-06-22: v3.26.1 — removed the opt-in `--dynamic` execution mode + the entire native-Workflow-engine path (single-issue span + megatask lanes); deleted `skills/worktask/references/dynamic-workflow.md` & `skills/megatask/references/dynamic-megatask.md`; dropped `execution_mode` PL0 metadata, the `state.json.workflow` schema block, the `workflow_launched`/`workflow_returned`/`dynamic_fallback`/`workflow_agent_stopped` audit actions, and the F5 dynamic-mode fallback row; scrubbed refs across `commands/worktask.md` (Options row, Step B block, See-Also), `skills/worktask/SKILL.md` (budget subsection + loop preamble), `skills/agent-coordination/SKILL.md` (fan-out section + audit rows), `handoff-protocol.md`/`resume.md`/`fn-gate.md`/`model-selection.md`/`cc-update.md`/`megatask` See-Alsos + `plugin.json` keywords. Manual Task-based loop is now the only execution path (already the default). PL0 dynamic **sizing** (complexity-scored stage selection) deliberately untouched. plugin.json 3.26.0→3.26.1. Docs/metadata only, ~13 files + 2 deletions.
- 2026-06-22: v3.26.0 — extracted milestone orchestration out of `/worktask` into a new **`/megatask`** command (`commands/megatask.md`): `/megatask N` (GitHub milestone) and `/megatask --issues 12,15,18` (explicit issue array). `--milestone:N`/`:ISSUE` REMOVED from `/worktask` entirely (now strictly single-issue, milestone-agnostic; gate-bypass triples in `worktask.md` drop `--milestone:N` — `/megatask` stamps `plan_gate`/`fn_gate:"bypass"` directly on each per-issue PL0). Megatask parses `Depends on: #N`/`Blocks: #M` from issue bodies + P0–P3 labels → builds a **dependency/blocker DAG** (new `skills/megatask/references/dependency-graph.md`, Kahn cycle-detection + levelled schedule), executes in topological+priority order, never starts an issue whose blockers are unmerged. Skill **renamed `skills/worktask-milestone`→`skills/megatask`** (slug `milestone-worktask`→`megatask`, v0.2.0); orchestrator schema → v3.1 (`blocked_by`/`blocks`/`level`/`topological_order`/`dependency_warnings`). New managed hook **`hooks/megatask-monitor.sh`** (SubagentStop, `continueOnBlock`, exit-0-always, self-skips w/o `orchestrator.json`): reconciles per-issue completion via `workspace.json.execution.status`, marks done, **unblocks dependents**, frees track, writes `megatask_progress` audit + terminal notification (self-test passes). `/pm-milestone` downstream pointer → `/megatask`. plugin.json + marketplace.json bumped 3.25.0→3.26.0, hook + command + skill registered. ~20 files across commands/, skills/, agents/, hooks/, manifests, README, MEMORY.
- 2026-06-23: product-manager 0.5.3 — Figma frames now reliably persist to `.context/designs/` (not `.context/images/`): `commands/worktask.md` Phase-1 init mkdir + Pre-work Prohibition allowlist now include `.context/designs`; product-manager.md frontmatter grants `Bash(mkdir:*)` (so PL0 can create the canonical dir before `curl -o`, which can't create parent dirs) with rationale comment; `skills/shared/figma-capture.md § Capture Workflow` adds a defensive `mkdir -p .context/designs` (once per turn) before the first curl so the standalone-Read doc is self-contained. `.context/designs` stays plural. Markdown-only; per-agent patch bump 0.5.2 → 0.5.3.
## Token Baselines

Authoritative per-surface baselines: `skills/cost-optimization/references/token-baselines.md`. This file no longer mirrors them.
